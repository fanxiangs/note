## 第7章

记忆

系统主内存存储应用程序和内核指令、它们的工作数据以及文件系统缓存。该数据的辅助存储通常是存储设备（磁盘），其运行速度要慢几个数量级。一旦主存储器已满，系统就可以开始在主存储器和存储设备之间切换数据。这是一个缓慢的过程，通常会成为系统瓶颈，从而显着降低性能。系统还可能终止最大消耗内存的进程，导致应用程序中断。

其他需要考虑的性能因素包括分配和释放内存、复制内存以及管理内存地址空间映射的 CPU 开销。在多套接字架构上，内存局部性可能成为一个因素，因为连接到本地套接字的内存比远程套接字具有更低的访问延迟。

本章的学习目标是：

*   了解内存概念。
    
*   熟悉内存硬件内部结构。
    
*   熟悉内核和用户分配器的内部结构。
    
*   具备 MMU 和 TLB 的应用知识。
    
*   遵循不同的内存分析方法。
    
*   描述系统范围和每个进程的内存使用情况。
    
*   识别由可用内存不足引起的问题。
    
*   定位进程地址空间和内核板中的内存使用情况。
    
*   使用分析器、跟踪器和火焰图调查内存使用情况。
    
*   了解内存的可调参数。
    

本章分为五个部分，前三个部分提供内存分析的基础，后两个部分展示其在基于 Linux 的系统中的实际应用。各部分如下：

*   背景介绍内存相关术语和关键内存性能概念。
    
*   架构提供了硬件和软件内存架构的通用描述。
    
*   方法论解释了绩效分析方法。
    
*   可观察性工具描述了用于内存分析的性能工具。
    
*   调优解释了调优和可调参数示例。
    

CPU 上的内存缓存（1/2/3 级，TLB）在第 6 章“CPU”中介绍。

### 7.1 术语

作为参考，本章中使用的与内存相关的术语包括以下内容：

*   主存储器：也称为物理存储器，描述计算机的快速数据存储区域，通常称为 DRAM。
    
*   虚拟内存：主内存的抽象，（几乎）无限且无竞争。虚拟内存不是真实内存。
    
*   常驻内存：当前驻留在主存中的内存。
    
*   匿名内存：没有文件系统位置或路径名的内存。它包括进程地址空间（称为堆）的工作数据。
    
*   地址空间：内存上下文。每个进程和内核都有虚拟地址空间。
    
*   段：标记为特定用途的虚拟内存区域，例如用于存储可执行或可写页面。
    
*   指令文本：指内存中的CPU指令，通常在一个段中。
    
*   OOM：内存不足，当内核检测到可用内存不足时。
    
*   页：操作系统和 CPU 使用的内存单位。从历史上看，它是 4 或 8 KB。现代处理器具有对更大尺寸的多种页面尺寸支持。
    
*   页面错误：无效的内存访问。这些是使用按需虚拟内存时的正常现象。
    
*   分页：主存和存储设备之间的页面传输。
    
*   交换：Linux 使用术语交换来指代交换设备的匿名分页（交换页的传输）。在 Unix 和其他操作系统中，交换是主内存和交换设备之间整个进程的传输。本书使用该术语的 Linux 版本。
    
*   交换：用于分页匿名数据的磁盘区域。它可能是存储设备上的一个区域，也称为物理交换设备，也可能是文件系统文件，称为交换文件。有些工具使用术语“交换”来指代虚拟内存（这是令人困惑且不正确的）。
    

本章将介绍其他术语。术语表包括供需要时参考的基本术语，包括地址、缓冲区和 DRAM。另请参阅第 2 章和第 3 章中的术语部分。

### 7.2 概念

以下是有关内存和内存性能的一些重要概念。

#### 7.2.1 虚拟内存

虚拟内存是一种抽象，它为每个进程和内核提供自己的大型、线性和私有地址空间。它简化了软件开发，将物理内存放置留给操作系统来管理。它还支持多任务处理（虚拟地址空间通过设计分开）和超额订阅（使用中的内存可以扩展到主内存之外）。虚拟内存在第 3 章“操作系统”第 3.2.8 节“虚拟内存”中介绍。有关历史背景，请参阅\[Denning 70\]。

图 7.1 显示了在具有交换设备（辅助存储）的系统上虚拟内存对于进程的作用。显示了内存页面，因为大多数虚拟内存实现都是基于页面的。

![Images](/Systems%20Performance_%20Enterprise%20and%20the%20Cl%20-%20Brendan%20Gregg/images/07fig01.jpg)

图7.1 进程虚拟内存

进程地址空间由虚拟内存子系统映射到主内存和物理交换设备。内核可以根据需要在它们之间移动内存页面，Linux 称为交换（其他操作系统称为匿名分页）。这允许内核超额订阅主内存。

内核可能会对超额订阅施加限制。常用的限制是主内存加上物理交换设备的大小。尝试超出此限制的内核分配可能会失败。这种“虚拟内存不足”错误乍一看可能会令人困惑，因为虚拟内存本身是一种抽象资源。

Linux 还允许其他行为，包括对内存分配不设限制。这称为过量使用，并在以下有关分页和请求调页的部分之后进行描述，这是过量使用工作所必需的。

#### 7.2.2 寻呼

分页是将页面移入和移出主存储器，分别称为页入和页出。它于 1962 年首次由 Atlas Computer 引入 \[Corbató 68\]，允许：

*   要执行的部分加载的程序
    
*   大于主存执行的程序
    
*   程序在主内存和存储设备之间高效移动
    

这些能力在今天仍然有效。与早期交换整个程序的技术不同，分页是一种管理和释放主内存的细粒度方法，因为页面大小单位相对较小（例如 4 KB）。

虚拟内存分页（分页虚拟内存）通过 BSD \[Babaoglu 79\] 引入 Unix 并成为标准。

随着后来添加用于共享文件系统页面的页面缓存（请参阅第 8 章，文件系统），两种不同类型的分页变得可用：文件系统分页和匿名分页。

##### 文件系统分页

文件系统分页是由内存映射文件中页面的读写引起的。对于使用文件内存映射 (mmap(2)) 的应用程序和使用页面缓存的文件系统（大多数这样做；请参阅第 8 章，文件系统），这是正常行为。它被称为“好的”寻呼 \[McDougall 06a\]。

当需要时，内核可以通过调出一些内存来释放内存。这就是术语有点棘手的地方：如果主内存中的文件系统页面被修改（称为脏页面），则页面调出将要求将其写入磁盘。相反，如果文件系统页面未被修改（称为清理），则页面调出仅释放内存以供立即重用，因为磁盘上已存在副本。因此，术语“页出”意味着页面被移出内存，这可能包括也可能不包括对存储设备的写入（您可能会在其他文本中看到术语“页出”的不同定义）。

##### 匿名寻呼（交换）

匿名分页涉及进程私有的数据：进程堆和堆栈。它被称为匿名是因为它在操作系统中没有命名位置（即没有文件系统路径名）。匿名页面调出需要将数据移动到物理交换设备或交换文件。 Linux 使用术语交换来指代这种类型的分页。

匿名寻呼会损害性能，因此被称为“坏”寻呼 \[McDougall 06a\]。当应用程序访问已调出的内存页时，它们会阻塞将它们读回主内存所需的磁盘 I/O。[^1]这是匿名页调入，会给应用程序带来同步延迟。匿名页面调出可能不会直接影响应用程序性能，因为它们可以由内核异步执行。

[^1]如果使用更快的存储设备作为交换设备，例如具有低于 10 μs 延迟的 3D XPoint，交换可能不会像以前那样“糟糕”分页，而是成为有意扩展主内存的简单方法，一款拥有成熟内核支持的。

当没有匿名分页（交换）时，性能最佳。这可以通过将应用程序配置为保留在可用主内存内并监视页面扫描、内存利用率和匿名分页来实现，以确保没有内存短缺的迹象。

#### 7.2.3 请求分页

支持按需调页的操作系统（大多数都支持）按需将虚拟内存页面映射到物理内存，如图 7.2 所示。这会将创建映射的 CPU 开销推迟到实际需要和访问它们时，而不是在首次分配内存范围时。

![Images](/Systems%20Performance_%20Enterprise%20and%20the%20Cl%20-%20Brendan%20Gregg/images/07fig02.jpg)

图 7.2 页面错误示例

图 7.2 中所示的序列首先提供分配的内存的 malloc()（步骤 1），然后是指向新分配的内存的存储指令（步骤 2）。为了让 MMU 确定存储的主内存位置，它对内存页面执行虚拟到物理的查找（步骤 3），但由于尚未存在映射而失败。这种失败称为页面错误（步骤 4），它会触发内核创建按需映射（步骤 5）。一段时间后，内存页面可以被调出到交换设备以释放内存（步骤 6）。

对于映射文件，步骤 2 也可以是加载指令，该文件应包含数据但尚未映射到该进程地址空间。

如果可以从内存中的另一个页面满足映射，则称为小错误。在进程的内存增长期间，从可用内存映射新页面时可能会发生这种情况（如图所示）。它也可能发生在映射到另一个现有页面时，例如从映射的共享库读取页面。

需要访问存储设备（图中未显示）的页面错误（例如访问未缓存的内存映射文件）称为主要错误。

虚拟内存模型和需求分配的结果是虚拟内存的任意页都可能处于以下状态之一：

1.  未分配
    
2.  已分配，但未映射（未填充且尚未出现故障）
    
3.  分配并映射到主内存 (RAM)
    
4.  分配并映射到物理交换设备（磁盘）
    

如果页面由于系统内存压力而被调出，则达到状态 (D)。从 (B) 到 (C) 的转换是页错误。如果需要磁盘 I/O，则为主要页面错误；否则，会出现轻微页面错误。

从这些状态中，还可以定义两个内存使用术语：

*   驻留集大小（RSS）：分配的主内存页的大小（C）
    
*   虚拟内存大小：所有分配区域的大小（B + C + D）
    

请求分页与分页虚拟内存一起通过 BSD 添加到 Unix。它已成为标准并被 Linux 使用。

#### 7.2.4 过度使用

Linux 支持过量使用的概念，它允许分配比系统可能存储的内存更多的内存，比物理内存和交换设备的总和还要多。它依赖于需求分页以及应用程序不使用其分配的大量内存的趋势。

通过过量使用，应用程序对内存的请求（例如，malloc(3)）将会成功，否则它们会失败。应用程序程序员可以慷慨地分配内存，然后根据需要稀疏地使用它，而不是保守地分配内存以保持在虚拟内存限制内。

在 Linux 上，可以使用可调参数配置过量使用的行为。有关详细信息，请参阅第 7.6 节“调整”。过度使用的后果取决于内核如何管理内存压力；请参阅第 7.3 节“架构”中对 OOM 杀手的讨论。

#### 7.2.5 进程交换

进程交换是整个进程在主内存和物理交换设备或交换文件之间的移动。这是管理主内存的原始 Unix 技术，也是术语“交换”的起源 \[Thompson 78\]。

要换出进程，必须将其所有私有数据写入交换设备，包括进程堆（匿名数据）、其打开的文件表以及仅在进程处于活动状态时才需要的其他元数据。源自文件系统且未经修改的数据可以在需要时删除并从原始位置再次读取。

进程交换会严重损害性能，因为已换出的进程需要大量磁盘 I/O 才能再次运行。对于当时的机器来说，它在早期的 Unix 上更有意义，例如 PDP-11，它的最大进程大小为 64 KB \[Bach 86\]。 （现代系统允许以 GB 为单位测量进程大小。）

此描述是为了提供历史背景。 Linux 系统根本不交换进程，仅依赖分页。

#### 7.2.6 文件系统缓存使用

系统启动后内存使用量增加是正常现象，因为操作系统使用可用内存来缓存文件系统，从而提高性能。原则是：如果有空闲的主内存，就将其用于有用的事情。这可能会让天真的用户感到困扰，因为他们会在启动后发现可用内存缩减到接近于零。但它不会给应用程序带来问题，因为当应用程序需要时，内核应该能够快速从文件系统缓存中释放内存。

有关可以消耗主内存的各种文件系统缓存的更多信息，请参阅第 8 章，文件系统。

#### 7.2.7 利用率和饱和度

主内存利用率可以计算为已用内存与总内存之比。文件系统缓存使用的内存可以视为未使用，因为它可供应用程序重用。

如果对内存的需求超过主内存的容量，主内存就会饱和。然后，操作系统可以通过使用分页、进程交换（如果支持）以及 Linux 上的 OOM Killer（稍后描述）来释放内存。任何这些活动都是主内存饱和的指标。

如果系统对其愿意分配的虚拟内存量施加限制（Linux 过量使用则没有），则还可以根据容量利用率来研究虚拟内存。如果是这样，一旦虚拟内存耗尽，内核将无法分配；例如，malloc(3) 失败，errno 设置为 ENOMEM。

请注意，系统上当前可用的虚拟内存有时（令人困惑地）称为可用交换区。

#### 7.2.8 分配器

虽然虚拟内存处理物理内存的多任务处理，但虚拟地址空间内的实际分配和放置通常由分配器处理。这些是用户态库或基于内核的例程，它们为软件程序员提供了一个简单的内存使用界面（例如，malloc(3)、free(3)）。

分配器可以对性能产生重大影响，并且系统可以提供多个用户级分配器库以供选择。它们可以通过使用包括每线程对象缓存在内的技术来提高性能，但如果分配变得碎片化和浪费，它们也会损害性能。第 7.3 节“架构”中介绍了具体示例。

#### 7.2.9 共享内存

内存可以在进程之间共享。这通常用于系统库，通过与使用它的所有进程共享一份只读指令文本副本来节省内存。

这给显示每个进程主内存使用情况的可观察性工具带来了困难。报告进程的总内存大小时是否应该包括共享内存？ Linux 使用的一项技术是提供额外的衡量标准，即比例集大小 (PSS)，其中包括私有内存（非共享）加上共享内存除以用户数量。请参阅第 7.5.9 节，pmap，了解可以显示 PSS 的工具。

#### 7.2.10 工作集大小

工作集大小 (WSS) 是进程经常用于执行工作的主内存量。对于内存性能调整来说，这是一个有用的概念：如果 WSS 可以适应 CPU 缓存而不是主内存，性能应该会大大提高。此外，如果 WSS 超过主内存大小，性能将大大降低，并且应用程序必须交换才能执行工作。

虽然作为一个概念很有用，但在实践中很难测量：可观测性工具中没有 WSS 统计数据（它们通常报告 RSS，而不是 WSS）。第 7.4.10 节“内存收缩”描述了 WSS 估计的实验方法，第 7.5.12 节“wss”展示了一个实验性工作集大小估计工具 wss(8)。

#### 7.2.11 字长

正如第 6 章中所介绍的，CPU、处理器可以支持多种字大小，例如 32 位和 64 位，从而允许任一软件运行。由于地址空间大小受到字大小可寻址范围的限制，因此需要 4 GB 以上内存的应用程序对于 32 位地址空间来说太大，需要针对 64 位或更高位进行编译。[^2]

[^2]x86 还具有物理地址扩展 (PAE) 功能（解决方法），允许 32 位处理器访问更大的内存范围（但不能在单个进程中访问）。

根据内核和处理器的不同，某些地址空间可能会保留给内核地址，并且不能供应用程序使用。一个极端的情况是具有 32 位字大小的 Windows，默认情况下为内核保留 2 GB，只为应用程序保留 2 GB \[Hall 09\]。在 Linux（或启用了 /3GB 选项的 Windows）上，内核预留空间为 1 GB。对于 64 位字大小（如果处理器支持的话），地址空间要大得多，因此内核预留不应该成为问题。

根据 CPU 架构，使用更大的位宽也可以提高内存性能，因为指令可以对更大的字长进行操作。如果数据类型具有较大位宽的未使用位，则可能会浪费少量内存。

### 7.3 架构

本节介绍内存架构，包括硬件和软件，包括处理器和操作系统的详细信息。

这些主题已被总结为性能分析和调优的背景。有关更多详细信息，请参阅本章末尾列出的供应商处理器手册和有关操作系统内部结构的文本。

#### 7.3.1 硬件

内存硬件包括主存、总线、CPU 缓存和 MMU。

##### 主内存

当今使用的常见主存储器类型是动态随机存取存储器（DRAM）。这是一种易失性存储器，断电时其内容就会丢失。 DRAM 提供高密度存储，因为每个位仅使用两个逻辑组件来实现：电容器和晶体管。电容器需要定期刷新以保持电量。

企业服务器根据其用途配置不同数量的 DRAM，通常范围从 1 GB 到 1 TB 甚至更大。云计算实例通常较小，每个实例在 512 MB 到 256 GB 之间。[^3]但是，云计算旨在将负载分散到实例池上，因此它们可以共同为分布式应用程序带来更多的在线 DRAM，尽管一致性成本要高得多。

[^3]例外情况包括 AWS EC2 高内存实例，其内存达到 24 TB \[Amazon 20\]。

###### 延迟

主存储器的访问时间可以用列地址选通 (CAS) 延迟来衡量：向存储器模块发送所需地址（列）与数据可供读取之间的时间。这取决于内存类型（对于 DDR4，约为 10 至 20 纳秒 \[Crucial 18\]）。对于存储器 I/O 传输，对于存储器总线（例如，64 位宽）传输高速缓存行（例如，64 字节宽）而言，该延迟可能会发生多次。 CPU 和 MMU 读取新的可用数据时还存在其他延迟。读取指令从 CPU 缓存返回时可避免这些延迟；如果处理器支持回写缓存（例如英特尔处理器），写入指令也可以避免它们。

##### 主存架构

图 7.3 显示了通用双处理器统一内存访问 (UMA) 系统的主内存架构示例。

![Images](/Systems%20Performance_%20Enterprise%20and%20the%20Cl%20-%20Brendan%20Gregg/images/07fig03.jpg)

图 7.3 UMA 主内存架构示例，双处理器

每个 CPU 通过共享系统总线对所有内存具有统一的访问延迟。当由跨所有处理器统一运行的单个操作系统内核实例管理时，这也是对称多处理 (SMP) 架构。

为了进行比较，图 7.4 显示了一个示例双处理器非均匀内存访问 (NUMA) 系统，该系统使用成为内存架构一部分的 CPU 互连。对于这种架构，主内存的访问时间根据其相对于 CPU 的位置而变化。

![Images](/Systems%20Performance_%20Enterprise%20and%20the%20Cl%20-%20Brendan%20Gregg/images/07fig04.jpg)

图 7.4 示例 NUMA 主内存架构，双处理器

CPU 1 可以通过其内存总线直接对 DRAM A 执行 I/O。这称为本地存储器。 CPU 1 通过 CPU 2 和 CPU 互连（两跳）执行到 DRAM B 的 I/O。这被称为远程存储器并且具有较高的访问延迟。

连接到每个 CPU 的内存组称为内存节点，或简称为节点。操作系统可以基于处理器提供的信息了解存储器节点拓扑。然后，它允许它根据内存局部性分配内存和调度线程，尽可能支持本地内存以提高性能。

##### 巴士

主内存如何物理连接到系统取决于主内存架构，如前图所示。实际的实现可能涉及 CPU 和内存之间的附加控制器和总线。主存储器可以通过以下方式之一访问：

*   共享系统总线：单处理器或多处理器，通过共享系统总线、内存桥控制器，最后是内存总线。这在第 6 章“CPU”中被描绘为 UMA 示例（图 7.3）和 Intel 前端总线示例（图 6.9）。该示例中的内存控制器是北桥。
    
*   直接：通过内存总线直接连接内存的单处理器。
    
*   互连：多处理器，每个处理器都通过内存总线直接连接内存，处理器通过 CPU 互连连接。这在之前的图 7.4 中被描述为 NUMA 示例； CPU 互连将在第 6 章“CPU”中讨论。
    

如果您怀疑您的系统不属于上述任何一种情况，请找到系统功能图并跟踪 CPU 和内存之间的数据路径，并记下沿途的所有组件。

###### DDR内存

对于任何架构，内存总线的速度通常由处理器和系统板支持的内存接口标准决定。自 1996 年以来使用的通用标准是双倍数据速率同步动态随机存取存储器 (DDR SDRAM)。术语双倍数据速率是指在时钟信号的上升和下降沿传输数据（也称为双泵浦）。术语“同步”是指内存与 CPU 同步计时。

DDR SDRAM 标准示例如表 7.1 所示。

表 7.1 DDR 带宽示例

标准

规格年份

内存时钟 (MHz)

数据速率（MT/秒）

峰值带宽（MB/秒）

DDR-200

2000

100

200

1,600

DDR-333

2000

167

333

2,667

DDR2-667

2003

167

667

5,333

DDR2-800

2003

200

800

6,400

DDR3-1333

2007

167

1,333

10,667

DDR3-1600

2007

200

1,600

12,800

DDR4-3200

2012

200

3,200

25,600

DDR5-4800

2020

200

4,800

38,400

DDR5-6400

2020

200

6,400

51,200

JEDEC 固态技术协会预计将于 2020 年发布 DDR5 标准。这些标准也使用“PC-”后跟每秒兆字节的数据传输率来命名，例如 PC-1600。

###### 多通道

系统架构可以支持并行使用多个存储器总线，以提高带宽。公倍数有双通道、三通道和四通道。例如，Intel Core i7 处理器最多支持四通道 DDR3-1600，最大内存带宽为 51.2 GB/s。

##### CPU缓存

处理器通常包括片上硬件缓存以提高内存访问性能。缓存可能包括以下级别，速度逐渐降低，大小逐渐增加：

*   Level 1：通常分为单独的指令缓存和数据缓存
    
*   2 级：指令和数据的缓存
    
*   第 3 级：另一个更大级别的缓存
    

根据处理器的不同，级别 1 通常由虚拟内存地址引用，级别 2 及以上由物理内存地址引用。

这些缓存在第 6 章“CPU”中进行了更深入的讨论。本章讨论另一种类型的硬件高速缓存，即 TLB。

##### 内存管理单元

MMU（内存管理单元）负责虚拟地址到物理地址的转换。这些是按页执行的，并且直接映射页内的偏移量。 MMU 在第 6 章“CPU”中在附近 CPU 缓存的上下文中介绍。

通用 MMU 如图 7.5 所示，具有 CPU 缓存和主内存级别。

![Images](/Systems%20Performance_%20Enterprise%20and%20the%20Cl%20-%20Brendan%20Gregg/images/07fig05.jpg)

图7.5 内存管理单元

###### 多种页面尺寸

现代处理器支持多种页面大小，这允许操作系统和 MMU 使用不同的页面大小（例如 4 KB、2 MB、1 GB）。 Linux 大页面功能支持更大的页面大小，例如 2 MB 或 1 GB。

##### TLB

图 7.5 中的 MMU 使用 TLB（转换后备缓冲区）作为第一级地址转换缓存，后面是主存中的页表。 TLB 可以分为指令页和数据页的单独高速缓存。

由于 TLB 用于映射的条目数量有限，因此使用较大的页面大小会增加可从其缓存转换的内存范围（其范围），从而减少 TLB 未命中并提高系统性能。 TLB 可以针对每个页面大小进一步划分为单独的高速缓存，从而提高在高速缓存中保留较大映射的可能性。

作为 TLB 大小的示例，典型的 Intel Core i7 处理器提供表 7.2 \[Intel 19a\] 中所示的四个 TLB。

表 7.2 典型 Intel Core i7 处理器的 TLB

类型

页面尺寸

参赛作品

操作说明

4K

每个线程 64 个，每个核心 128 个

操作说明

大的

每个线程 7 个

数据

4K

64

数据

大的

32

该处理器具有一级数据 TLB。 Intel Core 微架构支持两级，类似于 CPU 提供多级主内存缓存的方式。

TLB 的确切组成特定于处理器类型。有关处理器中 TLB 的详细信息及其操作的更多信息，请参阅供应商处理器手册。

#### 7.3.2 软件

内存管理软件包括虚拟内存系统、地址转换、交换、分页和分配。本节包含与性能最相关的主题：释放内存、空闲列表、页面扫描、交换、进程地址空间和内存分配器。

##### 释放内存

当系统上的可用内存变低时，内核可以使用多种方法来释放内存，将其添加到空闲页面列表中。 Linux 中的这些方法如图 7.6 所示，按照可用内存减少时的一般顺序排列。

![Images](/Systems%20Performance_%20Enterprise%20and%20the%20Cl%20-%20Brendan%20Gregg/images/07fig06.jpg)

图7.6 Linux内存可用性管理

这些方法是：

*   空闲列表：未使用（也称为空闲内存）且可立即分配的页面列表。这通常被实现为多个空闲页面列表，每个位置组（NUMA）一个。
    
*   页面缓存：文件系统缓存。称为 swappiness 的可调参数设置系统应支持从页面缓存释放内存而不是交换的程度。
    
*   交换：这是由页面调出守护进程 kswapd 进行的分页，它会查找最近未使用的页面以添加到空闲列表中，包括应用程序内存。这些被调出，这可能涉及写入基于文件系统的交换文件或交换设备。当然，只有配置了交换文件或设备后，这才可用。
    
*   收获：当超过低内存阈值时，可以指示内核模块和内核平板分配器立即释放任何可以轻松释放的内存。这也称为收缩。
    
*   OOM 杀手：内存不足杀手将通过查找并杀死牺牲进程来释放内存，使用 select\_bad\_process() 找到该进程，然后通过调用 oom\_kill\_process() 杀死该进程。这可能会作为“内存不足：终止进程”消息记录在系统日志 (/var/log/messages) 中。
    

Linux swappiness 参数控制是否支持通过分页应用程序或通过从页面缓存回收内存来释放内存。它是 0 到 100 之间的数字（默认值为 60），其中较高的值有利于通过分页释放内存。控制这些内存释放技术之间的平衡可以通过保留热文件系统缓存同时调出冷应用程序内存来提高系统吞吐量\[Corbet 04\]。

询问如果没有配置交换设备或交换文件会发生什么也很有趣。这限制了虚拟内存大小，因此如果禁用了过度使用，内存分配将很快失败。在Linux上，这也可能意味着OOM杀手会更快被使用。

考虑一个存在内存无限增长问题的应用程序。对于交换，这可能首先由于分页而成为性能问题，这是实时调试问题的机会。如果没有交换，就没有分页宽限期，因此应用程序要么遇到“内存不足”错误，要么 OOM 杀手终止它。如果仅在使用数小时后才发现问题，这可能会延迟调试问题。

在 Netflix 云中，实例通常不使用交换，因此应用程序如果耗尽内存就会被 OOM 终止。应用程序分布在大量实例中，杀死一个 OOM 会导致流量立即重定向到其他运行状况良好的实例。这被认为比允许一个实例由于交换而缓慢运行更好。

当使用内存 cgroup 时，可以使用如图 7.6 所示的类似内存释放技术来管理 cgroup 内存。系统可能拥有大量可用内存，但正在交换或遇到 OOM 杀手，因为容器已耗尽其 cgroup 控制的限制 \[Evans 17\]。有关 cgroup 和容器的更多信息，请参阅第 11 章，云计算。

以下部分描述了空闲列表、获取和页面调出守护进程。

##### 空闲列表

最初的 Unix 内存分配器使用内存映射和首次适应扫描。随着 BSD 中分页虚拟内存的引入，添加了空闲列表和页出守护进程 \[Babaoglu 79\]。空闲列表（如图 7.7 所示）允许立即找到可用内存。

![Images](/Systems%20Performance_%20Enterprise%20and%20the%20Cl%20-%20Brendan%20Gregg/images/07fig07.jpg)

图7.7 空闲列表操作

释放的内存将添加到列表的头部以供将来分配。由页面调出守护进程释放的内存（并且可能仍然包含有用的缓存文件系统页面）被添加到尾部。如果在重用有用页面之前发生对这些页面之一的未来请求，则可以回收该页面并将其从空闲列表中删除。

基于 Linux 的系统仍在使用一种形式的空闲列表，如图 7.6 所示。空闲列表通常通过分配器使用，例如内核的slab分配器和用户空间的libc malloc()（有自己的空闲列表）。这些依次消耗页面，然后通过其分配器 API 公开它们。

Linux 使用伙伴分配器来管理页面。这为不同大小的内存分配提供了多个空闲列表，遵循二次方方案。术语“伙伴”是指查找空闲内存的相邻页面，以便可以将它们一起分配。有关历史背景，请参阅\[Peterson 77\]。

伙伴空闲列表位于以下层次结构的底部，从每个内存节点 pg\_data\_t 开始：

*   节点：内存组、NUMA 感知
    
*   区域：用于特定目的的内存范围（直接内存访问 \[DMA\]、[^4] 正常、highmem）
    
    [^4]尽管 ZONE\_DMA 可能会被删除 \[Corbet 18a\]。
    
*   迁移类型：不可移动、可回收、可移动等。
    
*   尺寸：页数的二次方
    

在节点空闲列表内进行分配可以提高内存局部性和性能。对于最常见的分配（单页），伙伴分配器为每个 CPU 保留单页列表，以减少 CPU 锁争用。

##### 收割

收获主要涉及从内核平板分配器缓存中释放内存。这些缓存包含平板大小块中未使用的内存，可供重用。收割将此内存返回给系统以进行页面分配。

在Linux上，内核模块还可以调用register\_shrinker()来注册特定函数以获取自己的内存。

##### 页面扫描

通过分页释放内存是由内核页面调出守护进程管理的。当空闲列表中的可用主内存低于阈值时，页面调出守护程序开始页面扫描。仅在需要时才进行页面扫描。正常平衡的系统可能不会经常进行页面扫描，并且可能只在短时间内进行页面扫描。

在 Linux 上，页调出守护进程称为 kswapd，它扫描非活动和活动内存的 LRU 页面列表以释放页面。它根据可用内存和两个阈值来唤醒以提供迟滞，如图 7.8 所示。

![Images](/Systems%20Performance_%20Enterprise%20and%20the%20Cl%20-%20Brendan%20Gregg/images/07fig08.jpg)

图 7.8 kswapd 唤醒和模式

一旦可用内存达到最低阈值，kswapd 将在前台运行，根据请求同步释放内存页，这种方法有时称为直接回收 \[Gorman 04\]。这个最低阈值是可调的 (vm.min\_free\_kbytes)，其他阈值基于它进行缩放（低阈值为 2 倍，高阈值为 3 倍）。对于具有超过 kswap 回收速度的高分配突发的工作负载，Linux 提供了额外的可调参数以进行更积极的扫描，vm.watermark\_scale\_factor 和 vm.watermark\_boost\_factor：请参阅第 7.6.1 节, 可调参数。

页面缓存具有单独的非活动页面和活动页面列表。它们以 LRU 方式运行，允许 kswapd 快速找到空闲页面。它们如图 7.9 所示。

![Images](/Systems%20Performance_%20Enterprise%20and%20the%20Cl%20-%20Brendan%20Gregg/images/07fig09.jpg)

图7.9 kswapd列表

kswapd 首先扫描非活动列表，然后根据需要扫描活动列表。术语“扫描”是指在遍历列表时检查页面：如果页面被锁定/脏，则该页面可能没有资格被释放。 kswapd 使用的术语“扫描”与原始 UNIX 页面调出守护程序执行的扫描具有不同的含义，后者扫描所有内存。

#### 7.3.3 进程虚拟地址空间

进程虚拟地址空间由硬件和软件管理，是根据需要映射到物理页的一系列虚拟页。地址被分成称为段的区域，用于存储线程堆栈、进程可执行文件、库和堆。 Linux 上 32 位进程的示例如图 7.10 所示，适用于 x86 和 SPARC 处理器。

![Images](/Systems%20Performance_%20Enterprise%20and%20the%20Cl%20-%20Brendan%20Gregg/images/07fig10.jpg)

图 7.10 进程虚拟内存地址空间示例

在 SPARC 上，内核驻留在单独的完整地址空间中（图 7.10 中未显示）。请注意，在 SPARC 上，无法仅根据指针值来区分用户地址和内核地址； x86 采用不同的方案，其中用户地址和内核地址不重叠。[^5]

[^5]请注意，对于 64 位地址，处理器可能不支持完整的 64 位范围：AMD 规范允许实现仅支持 48 位地址，其中未使用的高阶位设置为最后一位：这将创建两个可用的地址范围，称为规范地址，从 0 到 0x00007ffff，用于用户空间，以及0xffff80000 到 0xffff，用于内核空间。这就是 x86 内核地址以 0xffff 开头的原因。

程序可执行段包含单独的文本段和数据段。库还由单独的可执行文本和数据段组成。这些不同的段类型是：

*   可执行文本：包含进程的可执行CPU指令。这是从文件系统上二进制程序的文本段映射的。它是只读的，具有执行权限。
    
*   可执行数据：包含从二进制程序的数据段映射的初始化变量。它具有读/写权限，以便在程序运行时可以修改变量。它还有一个私有标志，以便修改不会刷新到磁盘。
    
*   堆：这是程序的工作内存，并且是匿名内存（无文件系统位置）。它根据需要增长并通过 malloc(3) 分配。
    
*   堆栈：正在运行的线程的堆栈，映射读/写。
    

库文本段可以由使用同一库的其他进程共享，每个进程都有库数据段的私有副本。

###### 堆增长

造成混乱的一个常见原因是堆的无休止增长。是内存泄漏吗？对于简单的分配器， free(3) 不会将内存返回给操作系统；相反，保留内存是为了将来的分配。这意味着进程驻留内存只能增长，这是正常的。进程减少系统内存使用的方法包括：

*   重新执行：调用 execve(2) 从空地址空间开始
    
*   内存映射：使用mmap(2)和munmap(2)，它将内存返回给系统
    

内存映射文件在第 8 章“文件系统”、第 8.3.10 节“内存映射文件”中进行了描述。

Glibc 通常在 Linux 上使用，是一种高级分配器，支持 mmap 操作模式，以及用于向系统释放可用内存的 malloc\_trim(3) 函数。当堆顶可用内存变大时，malloc\_trim(3) 由 free(3) 自动调用，[^6] 并使用 sbrk(2) 系统调用释放它。

[^6]大于 M\_TRIM\_THRESHOLD mallopt(3) 参数，默认情况下为 128 KB。

##### 分配器

有多种用于内存分配的用户级和内核级分配器。图7.11展示了分配器的作用，包括一些常见的类型。

![Images](/Systems%20Performance_%20Enterprise%20and%20the%20Cl%20-%20Brendan%20Gregg/images/07fig11.jpg)

图 7.11 用户级和内核级内存分配器

页面管理已在前面第 7.3.2 节“软件”的“空闲列表”下描述。

内存分配器功能可包括：

*   简单API：例如malloc(3)、free(3)。
    
*   高效的内存使用：在处理各种大小的内存分配时，内存使用可能会变得碎片化，其中有许多未使用的区域浪费了内存。分配器可以努力合并未使用的区域，以便更大的分配可以利用它们，从而提高效率。
    
*   性能：内存分配可能很频繁，并且在多线程环境中，由于同步原语的争用，它们的性能可能会很差。分配器可以设计为谨慎使用锁，还可以利用每线程或每 CPU 缓存来提高内存局部性。
    
*   可观察性：分配器可以提供统计信息和调试模式来显示它是如何使用的，以及哪些代码路径负责分配。
    

以下各节介绍了内核级分配器（slab 和 SLUB）以及用户级分配器（glibc、TCMalloc 和 jemalloc）。

###### 板坯

内核slab分配器管理特定大小的对象的缓存，允许它们快速回收，而无需页面分配的开销。这对于内核分配尤其有效，内核分配通常用于固定大小的结构。

作为内核示例，以下两行来自 ZFS arc.c[^7]：

[^7]我想到这些作为示例的唯一原因是因为我开发了代码。

点击这里查看代码图片

        df = kmem\_alloc(sizeof (l2arc\_data\_free\_t), KM\_SLEEP);
        head = kmem\_cache\_alloc(hdr\_cache, KM\_PUSHPAGE);

第一个是 kmem\_alloc()，显示了传统风格的内核分配，其大小作为参数传递。内核根据该大小将其映射到平板缓存（超大尺寸的处理方式不同，由超大竞技场处理）。第二个，kmem\_cache\_alloc()，直接在自定义的slab分配器缓存上运行，在本例中是(kmem\_cache\_t \*)hdr\_cache。

平板分配器是为 Solaris 2.4 \[Bonwick 94\] 开发的，后来通过称为 magazines \[Bonwick 01\] 的每 CPU 缓存进行了增强：

我们的基本方法是为每个 CPU 提供一个称为杂志的 M 元素缓存，类似于自动武器。在 CPU 需要重新加载之前，每个 CPU 的磁带盒都可以满足 M 次分配，即将空磁带盒更换为满的磁带盒。

除了高性能之外，原始的slab分配器还具有调试和分析功能，包括审计跟踪分配细节和堆栈跟踪。

平板分配已被各种操作系统采用。 BSD 有一个称为通用内存分配器 (UMA) 的内核板分配器，它非常高效且支持 NUMA。在 Linux 2.2 版本中，slab 分配器也被引入，多年来它一直是默认选项。 Linux 此后已将 SLUB 作为选项或默认选项。

###### 婚礼

Linux内核SLUB分配器基于slab分配器，旨在解决各种问题，特别是关于slab分配器的复杂性。改进包括删除对象队列和每 CPU 缓存，将 NUMA 优化留给页面分配器（请参阅前面的空闲列表部分）。

SLUB 分配器在 Linux 2.6.23 \[Lameter 07\] 中成为默认选项。

###### glibc

用户级 GNU libc 分配器基于 Doug Lea 的 dlmalloc。其行为取决于分配请求的大小。小分配由内存箱提供，其中包含类似大小的单元，可以使用类似伙伴的算法来合并这些单元。较大的分配可以使用树查找来有效地查找空间。非常大的分配切换到使用 mmap(2)。最终结果是一个高性能的分配器，受益于多种分配策略。

###### TCMalloc

TCMalloc 是用户级线程缓存 malloc，它使用每线程缓存进行小型分配，减少锁争用并提高性能 \[Ghemawat 07\]。定期垃圾收集将内存迁移回中央堆进行分配。

###### 杰马洛克

libjemalloc 最初是 FreeBSD 用户级 libc 分配器，也可用于 Linux。它使用多竞技场、每线程缓存和小对象板等技术来提高可扩展性并减少内存碎片。它可以使用mmap(2)和sbrk(2)来获取系统内存，优先使用mmap(2)。 Facebook 使用 jemalloc 并添加了分析和其他优化 \[Facebook 11\]。

### 7.4 方法论

本节介绍内存分析和调优的各种方法和练习。表 7.3 总结了这些主题。

表 7.3 内存性能方法

部分

方法论

类型

[[ch07.xhtml#ch07lev4sec1|7.4.1]]

工具方法

观察分析

[[ch07.xhtml#ch07lev4sec2|7.4.2]]

使用方法

观察分析

[[ch07.xhtml#ch07lev4sec3|7.4.3]]

表征用途

观察分析、容量规划

[[ch07.xhtml#ch07lev4sec4|7.4.4]]

周期分析

观察分析

[[ch07.xhtml#ch07lev4sec5|7.4.5]]

性能监控

观察分析、容量规划

[[ch07.xhtml#ch07lev4sec6|7.4.6]]

泄漏检测

观察分析

[[ch07.xhtml#ch07lev4sec7|7.4.7]]

静态性能调优

观察分析、容量规划

[[ch07.xhtml#ch07lev4sec8|7.4.8]]

资源控制

调音

[[ch07.xhtml#ch07lev4sec9|7.4.9]]

微基准测试

实验分析

[[ch07.xhtml#ch07lev4sec10|7.4.10]]

内存萎缩

实验分析

有关更多策略和其中许多策略的介绍，请参阅第 2 章“方法论”。

这些方法可以单独使用或组合使用。在解决内存问题时，我的建议是从以下策略开始，按顺序：性能监控、USE 方法和表征使用情况。

第 7.5 节“可观察性工具”展示了应用这些方法的操作系统工具。

#### 7.4.1 工具方法

工具方法是迭代可用工具、检查它们提供的关键指标的过程。这是一种简单的方法，可能会忽略您所拥有的工具提供的可见性较差或没有可见性的问题，并且执行起来可能非常耗时。

对于内存，工具方法可能涉及针对 Linux 检查以下内容：

*   页面扫描：寻找连续的页面扫描（超过 10 秒）作为内存压力的迹象。这可以使用 `sar -B` 并检查 `pgscan` 列来完成。
    
*   压力失速信息 (PSI)：`cat /proc/pressure/memory` (Linux 4.20+)，用于检查内存压力（饱和度）统计信息及其随时间的变化情况。
    
*   交换：如果配置了交换，则内存页面的交换（Linux 对交换的定义）进一步表明系统内存不足。您可以使用 vmstat(8) 并检查 `si` 和 `so` 列。
    
*   `vmstat`：运行 `vmstat 1` 并检查 `free` 列的可用内存。
    
*   OOM 杀手：这些事件可以在系统日志 /var/log/messages 中或从 dmesg(1) 中看到。搜索“内存不足”。
    
*   `top`：查看哪些进程和用户是最大的物理内存消耗者（常驻）和虚拟内存消耗者（有关列名称的信息，请参阅手册页，根据版本的不同而有所不同）。 top(1) 还总结了可用内存。
    
*   perf(1)/BCC/bpftrace：使用堆栈跟踪跟踪内存分配，以识别内存使用的原因。请注意，这可能会花费相当大的开销。一种更便宜但粗糙的解决方案是执行 CPU 分析（定时堆栈采样）并搜索分配代码路径。
    

有关每个工具的更多信息，请参阅第 7.5 节“可观测性工具”。

#### 7.4.2 使用方法

USE 方法用于在性能调查的早期识别所有组件的瓶颈和错误，然后再采取更深入、更耗时的策略。

检查系统范围内的：

*   利用率：有多少内存正在使用中，有多少内存可用。物理内存和虚拟内存都应该检查。
    
*   饱和度：作为缓解内存压力的措施而执行的页面扫描、分页、交换和 Linux OOM 杀手牺牲的程度。
    
*   错误：软件或硬件错误。
    

您可能需要首先检查饱和度，因为持续饱和是内存问题的迹象。这些指标通常可以从操作系统工具中轻松获得，包括用于交换统计信息的 vmstat(8) 和 sar(1)，以及用于 OOM 杀手牺牲的 dmesg(1)。对于配置有单独磁盘交换设备的系统，交换设备的任何活动都是内存压力的另一个迹象。 Linux 还提供内存饱和统计信息作为压力失速信息 (PSI) 的一部分。

不同的工具可以以不同的方式报告物理内存利用率，具体取决于它们是否考虑了未引用的文件系统缓存页面或非活动页面。系统可能会报告它只有 10 MB 的可用内存，而实际上它有 10 GB 的文件系统缓存，可以在需要时立即由应用程序回收。检查工具文档以了解其中包含的内容。

可能还需要检查虚拟内存利用率，具体取决于系统是否执行过量使用。对于不这样做的系统，一旦虚拟内存耗尽，内存分配就会失败——这是一种内存错误。

内存错误可能是由软件引起的，例如失败的内存分配或 Linux OOM Killer，也可能是由硬件引起的，例如 ECC 错误。从历史上看，内存分配错误一直由应用程序报告，尽管并非所有应用程序都这样做（并且，由于 Linux 过度使用，开发人员可能认为没有必要）。硬件错误也很难诊断。当使用 ECC 内存时，某些工具可能会报告 ECC 可纠正错误（例如，在 Linux 上，dmidecode(8)、edac-utils、`ipmitool sel`）。这些可纠正的错误可以用作 USE 方法错误度量，并且可能是不可纠正的错误可能很快发生的标志。对于实际的（不可纠正的）内存错误，您可能会遇到任意应用程序无法解释的、不可重现的崩溃（包括段错误和总线错误信号）。

对于实施内存限制或配额（资源控制）的环境（如在某些云计算环境中），可能需要以不同的方式测量内存利用率和饱和度。即使主机上有足够的物理内存，您的操作系统实例也可能处于软件内存限制和交换状态。请参阅第 11 章，云计算。

#### 7.4.3 表征用途

在进行容量规划、基准测试和模拟工作负载时，表征内存使用情况是一项重要的练习。它还可以通过查找和纠正错误配置来带来一些最大的性能提升。例如，数据库缓存可能配置得太小，命中率低，或者太大，导致系统分页。

对于内存，表征使用情况涉及识别内存的使用位置和数量：

*   系统范围的物理和虚拟内存利用率
    
*   饱和度：交换和 OOM 杀死
    
*   内核和文件系统缓存内存使用情况
    
*   每个进程的物理和虚拟内存使用情况
    
*   内存资源控制的使用（如果存在）
    

此示例描述显示了如何一起表达这些属性：

系统有 256 GB 的主内存，其中 1% 被进程使用（已利用），30% 被文件系统缓存使用。最大的进程是数据库，消耗 2 GB 主内存 (RSS)，这是其迁移来源的先前系统的配置限制。

随着更多内存用于缓存工作数据，这些特征可能会随着时间的推移而变化。除了常规缓存增长之外，内核或应用程序内存也可能由于内存泄漏（软件错误）而随着时间的推移而不断增长。

##### 高级使用分析/清单

此处列出了其他特征作为需要考虑的问题，也可以作为彻底研究内存问题时的清单：

*   应用程序的工作集大小 (WSS) 是多少？
    
*   内核内存用在哪里？每块板？
    
*   文件系统缓存有多少是活动的，而不是非活动的？
    
*   进程内存在哪里使用（指令、缓存、缓冲区、对象等）？
    
*   为什么进程要分配内存（调用路径）？
    
*   为什么内核要分配内存（调用路径）？
    
*   进程库映射有什么奇怪的吗（例如，随着时间的推移而变化）？
    
*   哪些进程正在被主动换出？
    
*   哪些流程之前已被换出？
    
*   进程或内核是否存在内存泄漏？
    
*   在 NUMA 系统中，内存在内存节点之间的分布情况如何？
    
*   IPC 和内存停顿周期率是多少？
    
*   内存总线的平衡程度如何？
    
*   与远程内存 I/O 相比，执行了多少本地内存 I/O？
    

以下部分可以帮助回答其中一些问题。请参阅第 2 章“方法论”，了解该方法论的高级摘要以及要衡量的特征（谁、为什么、什么、如何）。

#### 7.4.4 周期分析

内存总线负载可以通过检查 CPU 性能监控计数器 (PMC) 来确定，该计数器可以编程来计算内存停顿周期、内存总线使用情况等。首先的指标是每周期指令数 (IPC)，它反映了 CPU 负载对内存的依赖程度。请参阅第 6 章，CPU。

#### 7.4.5 性能监控

性能监控可以随着时间的推移识别活动问题和行为模式。内存的关键指标是：

*   利用率：使用百分比，可以从可用内存中推断出来
    
*   饱和：交换、OOM 杀死
    

对于实施内存限制或配额（资源控制）的环境，可能还需要收集与所施加的限制相关的统计信息。

还可以监视错误（如果可用），如第 7.4.2 节“使用方法”中的利用率和饱和度所述。

监控一段时间内的内存使用情况，尤其是按进程监控内存使用情况，可以帮助识别内存泄漏的存在和发生率。

#### 7.4.6 泄漏检测

当应用程序或内核模块无休止地增长，消耗来自空闲列表、文件系统缓存以及最终来自其他进程的内存时，就会出现此问题。这可能首先会被注意到，因为系统开始交换或应用程序被 OOM 终止，以应对无尽的内存压力。

此类问题由以下任一原因引起：

*   内存泄漏：一种软件错误，内存不再使用但从未释放。这可以通过修改软件代码或应用补丁或升级（修改代码）来解决。
    
*   内存增长：软件正常消耗内存，但消耗速度远高于系统所需的速度。通过更改软件配置或软件开发人员更改应用程序消耗内存的方式可以解决此问题。
    

内存增长问题经常被误认为是内存泄漏。首先要问的问题是：它应该这样做吗？检查内存使用情况、应用程序的配置及其分配器的行为。应用程序可以被配置为填充存储器高速缓存，并且观察到的增长可以是高速缓存预热。

如何分析内存泄漏取决于软件和语言类型。一些分配器提供调试模式来记录分配详细信息，然后可以在事后分析这些详细信息以识别负责的调用路径。某些运行时具有用于进行堆转储分析的方法，以及用于进行内存泄漏调查的其他工具。

Linux BCC 跟踪工具包括用于增长和泄漏分析的 memleak(8)：它跟踪分配并记录在一段时间内未释放的分配以及分配代码路径。它无法判断这些是泄漏还是正常增长，因此您的任务是分析代码路径以确定是哪种情况。 （请注意，该工具还会因高分配率而产生高开销。）BCC 在第 15 章 BPF、第 15.1 节 BCC 中介绍。

#### 7.4.7 静态性能调优

静态性能调优重点关注配置环境的问题。对于内存性能，请检查静态配置的以下方面：

*   主存总共有多少？
    
*   应用程序配置使用多少内存（它们自己的配置）？
    
*   应用程序使用哪些内存分配器？
    
*   主存的速度是多少？它是可用的最快类型 (DDR5)吗？
    
*   主内存是否已经过全面测试（例如，使用 Linux memtester）？
    
*   系统架构是怎样的？努玛，乌玛？
    
*   操作系统是否支持 NUMA？它提供 NUMA 可调参数吗？
    
*   内存是连接到同一个插槽，还是跨插槽分开？
    
*   有多少条内存总线？
    
*   CPU 高速缓存的数量和大小是多少？ TLB？
    
*   BIOS 设置是什么？
    
*   是否配置和使用了大页面？
    
*   过量使用是否可用并已配置？
    
*   正在使用哪些其他系统内存可调参数？
    
*   是否存在软件施加的内存限制（资源控制）？
    

回答这些问题可能会揭示被忽视的配置选择。

#### 7.4.8 资源控制

操作系统可以提供细粒度的控制以将存储器分配给进程或进程组。这些控制可能包括主存储器和虚拟存储器使用的固定限制。它们的工作方式是特定于实现的，并在第 7.6 节“调优”和第 11 章“云计算”中讨论。

#### 7.4.9 微基准测试

微基准测试可用于确定主存储器的速度和特征，例如CPU高速缓存和高速缓存行大小。这在分析系统之间的差异时可能会有所帮助，因为内存访问速度对性能的影响可能比 CPU 时钟速度更大，具体取决于应用程序和工作负载。

在第 6 章“CPU”中，“CPU 缓存”下的“延迟”部分（第 6.4.1 节“硬件”）显示了微基准测试内存访问延迟的结果，以确定 CPU 缓存的特性。

#### 7.4.10 内存收缩

这是一种使用负实验的工作集大小 (WSS) 估计方法，需要配置交换设备来执行实验。在测量性能和交换时，应用程序的可用主内存逐渐减少：性能急剧下降且交换大幅增加的点表明 WSS 不再适合可用内存。

虽然值得一提的是作为一个负面实验的例子，但不建议将其用于生产用途，因为它会故意损害性能。对于其他 WSS 估计技术，请参阅第 7.5.12 节中的实验性 wss(8) 工具 wss 和我关于 WSS 估计的网站 \[Gregg 18c\]。

### 7.5 可观测性工具

本节介绍基于 Linux 的操作系统的内存可观察性工具。请参阅上一节，了解使用它们时要遵循的方法。

本节中的工具如表 7.4 所示。

表 7.4 Linux 内存可观测性工具

部分

工具

描述

[[ch07.xhtml#ch07lev5sec1|7.5.1]]

起来

虚拟和物理内存统计

[[ch07.xhtml#ch07lev5sec2|7.5.2]]

PSI

内存压力停顿信息

[[ch07.xhtml#ch07lev5sec3|7.5.3]]

交换

交换设备使用情况

[[ch07.xhtml#ch07lev5sec4|7.5.4]]

萨尔

历史统计

[[ch07.xhtml#ch07lev5sec5|7.5.5]]

平板式

内核slab分配器统计信息

[[ch07.xhtml#ch07lev5sec6|7.5.6]]

编号的

NUMA 统计

[[ch07.xhtml#ch07lev5sec7|7.5.7]]

附注

进程状态

[[ch07.xhtml#ch07lev5sec8|7.5.8]]

顶部

监控每个进程的内存使用情况

[[ch07.xhtml#ch07lev5sec9|7.5.9]]

映射图

进程地址空间统计

[[ch07.xhtml#ch07lev5sec10|7.5.10]]

性能

内存 PMC 和跟踪点分析

[[ch07.xhtml#ch07lev5sec11|7.5.11]]

德斯努普

直接回收追踪

[[ch07.xhtml#ch07lev5sec12|7.5.12]]

沃斯

工作集大小估计

[[ch07.xhtml#ch07lev5sec13|7.5.13]]

bpftrace

用于内存分析的跟踪程序

这是支持第 7.4 节“方法论”的一系列工具和功能。我们从系统范围内存使用统计工具开始，然后深入到每个进程和分配跟踪。一些传统工具可能在其起源的其他类 Unix 操作系统上可用，包括：vmstat(8)、sar(1)、ps(1)、top(1) 和 pmap(1)。 drsnoop(8) 是来自 BCC（第 15 章）的 BPF 工具。

请参阅每个工具的文档，包括其手册页，以获取有关其功能的完整参考。

#### 7.5.1 起义

虚拟内存统计命令 vmstat(8) 提供系统内存运行状况的高级视图，包括当前可用内存和分页统计信息。还包括 CPU 统计信息，如第 6 章 CPU 中所述。

当 Bill Joy 和 Ozalp Babaoglu 于 1979 年为 BSD 引入它时，原始手册页包括：

错误：打印出的数字太多，有时很难弄清楚该看什么。

以下是 Linux 版本的输出示例：

点击这里查看代码图片

$ **vmstat 1**
procs -----------memory---------- ---swap-- -----io---- -system-- ----cpu----
 r  b   **swpd   free   buff  cache   si   so**    bi    bo   in   cs us sy id wa
 4  0      0 34454064 111516 13438596   0   0    0    5    2    0  0  0 100  0
 4  0      0 34455208 111516 13438596   0   0    0    0 2262 15303 16 12 73  0
 5  0      0 34455588 111516 13438596   0   0    0    0 1961 15221 15 11 74  0
 4  0      0 34456300 111516 13438596   0   0    0    0 2343 15294 15 11 73  0
\[...\]

此版本的 vmstat(8) 不会在输出的第一行打印 `procs` 或 `memory` 列的启动后摘要值，而是立即显示当前状态。默认情况下，列以千字节为单位：

*   `swpd`：换出的内存量
    
*   `free`：可用内存
    
*   `buff`：缓冲区高速缓存中的内存
    
*   `cache`：页面缓存中的内存
    
*   `si`：内存交换（分页）
    
*   `so`：内存换出（分页）
    

缓冲区和页缓存在第 8 章“文件系统”中描述。系统中的可用内存在启动后减少并被这些缓存使用以提高性能是正常的。需要时可以将其发布以供应用程序使用。

如果 `si` 和 `so` 列持续不为零，则系统面临内存压力并且正在交换到交换设备或文件（请参阅 swapon(8)）。其他工具，包括那些按进程显示内存的工具（例如 top(1)、ps(1)），可用于调查正在消耗内存的内容。

在具有大量内存的系统上，列可能会变得不对齐并且有点难以读取。您可以尝试使用 `-S` 选项将输出单位更改为兆字节（使用 `m` 表示 10000，使用 `M` 表示 1048576）：

点击这里查看代码图片

$ **vmstat -Sm 1**
procs -----------memory---------- ---swap-- -----io---- -system-- ----cpu----
 r  b   swpd   free   buff  cache   si   so    bi    bo   in   cs us sy id wa
 4  0      0  35280    114  13761    0    0     0     5    2    1  0  0 100  0
 4  0      0  35281    114  13761    0    0     0     0 2027 15146 16 13 70  0
\[...\]

还有一个 `-a` 选项可以打印页面缓存中非活动和活动内存的详细信息：

点击这里查看代码图片

$ **vmstat -a 1**
procs -----------memory---------- ---swap-- -----io---- -system-- ----cpu----
 r  b   swpd   free  **inact active**   si   so    bi    bo   in   cs us sy id wa
 5  0      0 34453536 10358040 3201540   0   0    0    5    2    0  0  0 100  0
 4  0      0 34453228 10358040 3200648   0   0    0    0 2464 15261 16 12 71  0
\[...\]

可以使用小写的 `-s` 选项将这些内存统计信息打印为列表。

#### 7.5.2 PSI

Linux 4.20 中添加的 Linux 压力失速信息 (PSI) 包括内存饱和度的统计信息。这些不仅显示是否存在内存压力，还显示过去五分钟内存压力的变化情况。输出示例：

点击这里查看代码图片

\# **cat /proc/pressure/memory**
some avg10=2.84 avg60=1.23 avg300=0.32 total=1468344
full avg10=1.85 avg60=0.66 avg300=0.16 total=702578

此输出显示内存压力正在增加，10 秒平均值 (2.84) 高于 300 秒平均值 (0.32)。这些平均值是任务内存停滞的时间百分比。 `some` 行显示某些任务（线程）何时受到影响，`full` 行显示何时所有可运行任务受到影响。

PSI 统计数据也按 cgroup2 进行跟踪（第 11 章云计算中介绍了 cgroup）\[Facebook 19\]。

#### 7.5.3 交换

swapon(1) 可以显示交换设备是否已配置以及其卷的使用量。例如：

点击这里查看代码图片

$ **swapon**
NAME      TYPE      SIZE   USED PRIO
/dev/dm-2 partition 980M 611.6M   -2
/swap1    file       30G  10.9M   -3

此输出显示两个交换设备：一个 980 MB 的物理磁盘分区和一个名为 /swap1 的 30 GB 文件。输出还显示两者的使用量。现在很多系统都没有配置swap；在这种情况下，swapon(1) 将不会打印任何输出。

如果交换设备具有活动 I/O，则可以在 vmstat(1) 的 `si` 和 `so` 列中看到，并在 iostat(1) 中看到设备 I/O（第 9 章）。

#### 7.5.4 SAR

系统活动报告器 sar(1) 可用于观察当前活动，并可配置为存档和报告历史统计数据。本书的各个章节都提到了它提供的不同统计数据，并在第 4 章“可观测性工具”第 4.4 节“sar”中介绍了它。

Linux 版本通过以下选项提供内存统计信息：

*   `-B`：寻呼统计信息
    
*   `-H`：大页统计信息
    
*   `-r`：内存利用率
    
*   `-S`：交换空间统计信息
    
*   `-W`：交换统计信息
    

这些涵盖内存使用情况、页面调出守护程序的活动以及大页面使用情况。有关这些主题的背景信息，请参阅第 7.3 节“架构”。

提供的统计数据包括表 7.5 中的数据。

表 7.5 Linux sar 内存统计

选项

统计

描述

单位

`-B`

`pgpgin/s`

翻页

千字节/秒

`-B`

`pgpgout/s`

页面输出

千字节/秒

`-B`

`fault/s`

主要故障和次要故障

计数/秒

`-B`

`majflt/s`

主要故障

计数/秒

`-B`

`pgfree/s`

页面已添加到空闲列表

计数/秒

`-B`

`pgscank/s`

由后台页出守护进程 (kswapd) 扫描的页面

计数/秒

`-B`

`pgscand/s`

直接页面扫描

计数/秒

`-B`

`pgsteal/s`

页面和交换缓存回收

计数/秒

`-B`

`%vmeff`

页面窃取/页面扫描的比率，显示页面回收效率

百分比

`-H`

`hbhugfree`

释放大页内存（大页大小）

千字节

`-H`

`hbhugused`

使用的大页内存

千字节

`-H`

`%hugused`

巨大的页面使用量

百分比

`-r`

`kbmemfree`

可用内存（完全未使用）

千字节

`-r`

`kbavail`

可用内存，包括可以轻松从页面缓存中释放的页面

千字节

`-r`

`kbmemused`

使用的内存（不包括内核）

千字节

`-r`

`%memused`

内存使用情况

百分比

`-r`

`kbbuffers`

缓冲区高速缓存大小

千字节

`-r`

`kbcached`

页面缓存大小

千字节

`-r`

`kbcommit`

已提交的主内存：服务当前工作负载所需的内存量的估计

千字节

`-r`

`%commit`

主内存致力于当前工作负载，估计

百分比

`-r`

`kbactive`

活动列表内存大小

千字节

`-r`

`kbinact`

非活动列表内存大小

千字节

`-r`

`kbdirtyw`

修改内存写入磁盘

千字节

`-r ALL`

`kbanonpg`

进程匿名内存

千字节

`-r ALL`

`kbslab`

内核slab缓存大小

千字节

`-r ALL`

`kbkstack`

内核堆栈空间大小

千字节

`-r ALL`

`kbpgtbl`

最低级页表大小

千字节

`-r ALL`

`kbvmused`

使用的虚拟地址空间

千字节

`-S`

`kbswpfree`

可用交换空间

千字节

`-S`

`kbswpused`

已用交换空间

千字节

`-S`

`%swpused`

已用交换空间

百分比

`-S`

`kbswpcad`

缓存交换空间：它驻留在主内存和交换设备中，因此无需磁盘 I/O 即可调出页面

千字节

`-S`

`%swpcad`

缓存交换与已用交换的比率

百分比

`-W`

`pswpin/s`

页面调入（Linux“交换”）

页数/秒

`-W`

`pswpout/s`

页面调出（Linux“交换”）

页数/秒

许多统计信息名称都包含测量单位：`pg` 表示页数，`kb` 表示千字节，`%` 表示百分比，`/s` 表示每秒。有关完整列表，请参阅手册页，其中包括一些其他基于百分比的统计信息。

重要的是要记住，在需要时可以获得有关高级内存子系统的使用和操作的详细信息。要更深入地了解这些内容，您可能需要使用跟踪器来检测内存跟踪点和内核函数，例如以下部分中的 perf(1) 和 bpftrace。您还可以浏览mm中的源代码，特别是mm/vmscan.c。当开发人员讨论统计数据应该是什么时，linux-mm 邮件列表中有许多帖子提供了进一步的见解。

`%vmeff` 指标是衡量页面回收效率的有用指标。高表示页面被成功从非活动列表中窃取（健康）；低意味着系统正在挣扎。手册页描述了接近 100% 的高值，以及不到 30% 的低值。

另一个有用的指标是 pgscand，它有效地显示应用程序阻塞内存分配和进入直接回收的速率（越高越好）。要查看应用程序在直接回收事件期间花费的时间，您可以使用跟踪工具：请参阅第 7.5.11 节，drsnoop。

#### 7.5.5 平板

Linux的slabtop(1)命令从slab分配器中打印内核slab缓存的使用情况。与top(1)一样，它实时刷新屏幕。

这是一些示例输出：

点击这里查看代码图片

\# **slabtop -sc**
 Active / Total Objects (% used)    : 686110 / 867574 (79.1%)
 Active / Total Slabs (% used)      : 30948 / 30948 (100.0%)
 Active / Total Caches (% used)     : 99 / 164 (60.4%)
 Active / Total Size (% used)       : 157680.28K / 200462.06K (78.7%)
 Minimum / Average / Maximum Object : 0.01K / 0.23K / 12.00K
  OBJS ACTIVE  USE OBJ SIZE  SLABS OBJ/SLAB CACHE SIZE NAME
 45450  33712  74%    1.05K   3030       15     48480K ext4\_inode\_cache
161091  81681  50%    0.19K   7671       21     30684K dentry
222963 196779  88%    0.10K   5717       39     22868K buffer\_head
 35763  35471  99%    0.58K   2751       13     22008K inode\_cache
 26033  13859  53%    0.57K   1860       14     14880K radix\_tree\_node
 93330  80502  86%    0.13K   3111       30     12444K kernfs\_node\_cache
  2104   2081  98%    4.00K    263        8      8416K kmalloc-4k
   528    431  81%    7.50K    132        4      4224K task\_struct
\[...\]

输出在顶部有一个摘要和一个板列表，包括它们的对象计数 (`OBJS`)、活跃数量 (`ACTIVE`)、使用百分比 (`USE`)、大小对象的数量（`OBJ SIZE`，字节），以及缓存的总大小（`CACHE SIZE`，字节）。在此示例中，使用 `-sc` 选项按缓存大小排序，最大的位于顶部：ext4\_inode\_cache。

lab统计信息来自/proc/slabinfo，也可以通过`vmstat -m`打印。

#### 7.5.6 编号

numastat(8)[^8] 工具提供非均匀内存访问 (NUMA) 系统（通常是具有多个 CPU 插槽的系统）的统计信息。以下是双插槽系统的一些输出示例：

[^8]起源：Andi Kleen 在 2003 年左右以 Perl 脚本形式编写了最初的 numastat 工具；当前版本由 Bill Gray 于 2012 年编写。

点击这里查看代码图片

\# **numastat**
                           node0           node1
numa\_hit            210057224016    151287435161
numa\_miss             9377491084       291611562
numa\_foreign           291611562      9377491084
interleave\_hit             36476           36665
local\_node          210056887752    151286964112
other\_node            9377827348       292082611

该系统有两个 NUMA 节点，一个用于连接到每个插槽的每个内存条。 Linux 尝试在最近的 NUMA 节点上分配内存，numastat(8) 显示了这是多么成功。主要统计数据是：

*   `numa_hit`：目标 NUMA 节点上的内存分配。
    
*   `numa_miss + numa_foreign`：内存分配不在首选 NUMA 节点上。 （`numa_miss` 显示本应在其他地方的本地分配，而 `numa_foreign` 显示本应在本地的远程分配。）
    
*   `other_node`：进程在其他地方运行时在此节点上的内存分配。
    

示例输出显示 NUMA 分配策略表现良好：与其他统计数据相比，命中率很高。如果命中率低得多，您可以考虑调整 sysctl(8) 中的 NUMA 可调参数，或使用其他方法来提高内存局部性（例如，对工作负载或系统进行分区，或选择具有较少 NUMA 节点的不同系统）。如果没有办法改进 NUMA，numastat(8) 至少可以帮助解释内存 I/O 性能不佳的原因。

numastat(8) 支持 `-n` 以 MB 为单位打印统计信息，并支持 `-m` 以 /proc/meminfo 的样式打印输出。根据您的 Linux 发行版，numastat(8) 可能在 numactl 软件包中提供。

#### 7.5.7 皮秒

进程状态命令 ps(1) 列出所有进程的详细信息，包括内存使用统计信息。它的用法已在第 6 章“CPU”中介绍。

例如，使用 BSD 风格的选项：

点击这里查看代码图片

$ **ps aux**
USER       PID %CPU %MEM    VSZ   RSS TTY  STAT START   TIME COMMAND
\[...\]
bind      1152  0.0  0.4 348916 39568 ?    Ssl  Mar27  20:17 /usr/sbin/named -u bind
root      1371  0.0  0.0  39004  2652 ?    Ss   Mar27  11:04 /usr/lib/postfix/master
root      1386  0.0  0.6 207564 50684 ?    Sl   Mar27   1:57 /usr/sbin/console-kit-daemon --no-daemon
rabbitmq  1469  0.0  0.0  10708   172 ?    S    Mar27   0:49 /usr/lib/erlang/erts-5.7.4/bin/epmd -daemon
rabbitmq  1486  0.1  0.0 150208  2884 ?    Ssl  Mar27 453:29 /usr/lib/erlang/erts-5.7.4/bin/beam.smp -W w -K true -A30 ...

此输出包括以下列：

*   `%MEM`：主内存使用量（物理内存，RSS）占系统总内存的百分比
    
*   `RSS`：驻留集大小（千字节）
    
*   `VSZ`：虚拟内存大小（KB）
    

虽然 `RSS` 显示主内存使用情况，但它包括共享内存段，例如系统库，可能由数十个进程映射。如果您要对 `RSS` 列进行求和，您可能会发现它超出了系统中的可用内存，因为该共享内存被过度计数。有关共享内存的背景信息，请参阅第 7.2.9 节“共享内存”，以及后面的 pmap(1) 命令以分析共享内存的使用情况。

可以使用 SVR4 样式的 `-o` 选项来选择这些列，例如：

点击这里查看代码图片

\# **ps -eo pid,pmem,vsz,rss,comm**
  PID %MEM  VSZ  RSS COMMAND
\[...\]
13419  0.0 5176 1796 /opt/local/sbin/nginx
13879  0.1 31060 22880 /opt/local/bin/ruby19
13418  0.0 4984 1456 /opt/local/sbin/nginx
15101  0.0 4580   32 /opt/riak/lib/os\_mon-2.2.6/priv/bin/memsup
10933  0.0 3124 2212 /usr/sbin/rsyslogd
\[...\]

Linux 版本还可以打印主要和次要故障的列（`maj_flt`、`min_flt`）。

ps(1) 的输出可以在内存列上进行后排序，以便可以快速识别最高消耗者。或者，尝试 top(1)，它提供交互式排序。

#### 7.5.8 顶部

top(1) 命令监视顶部运行的进程并包括内存使用统计信息。它在第 6 章“CPU”中介绍过。例如，在 Linux 上：

点击这里查看代码图片

$ **top -o %MEM**
top - 00:53:33 up 242 days,  2:38,  7 users,  load average: 1.48, 1.64, 2.10
Tasks: 261 total,   1 running, 260 sleeping,   0 stopped,   0 zombie
Cpu(s):  0.0%us,  0.0%sy,  0.0%ni, 99.9%id,  0.0%wa,  0.0%hi,  0.0%si,  0.0%st
**Mem:   8181740k total,  6658640k used,  1523100k free,   404744k buffers**
**Swap:  2932728k total,   120508k used,  2812220k free,  2893684k cached**

  PID USER      PR  NI  **VIRT  RES**  SHR S %CPU **%MEM**    TIME+  COMMAND
29625 scott     20   0 2983m 2.2g 1232 S   45 28.7  81:11.31 node
 5121 joshw     20   0  222m 193m  804 S    0  2.4 260:13.40 tmux
 1386 root      20   0  202m  49m 1224 S    0  0.6   1:57.70 console-kit-dae
 6371 stu       20   0 65196  38m  292 S    0  0.5  23:11.13 screen
 1152 bind      20   0  340m  38m 1700 S    0  0.5  20:17.36 named
15841 joshw     20   0 67144  23m  908 S    0  0.3 201:37.91 mosh-server
18496 root      20   0 57384  16m 1972 S    3  0.2   2:59.99 python
 1258 root      20   0  125m 8684 8264 S    0  0.1   2052:01 l2tpns
16295 wesolows  20   0 95752 7396  944 S    0  0.1   4:46.07 sshd
23783 brendan   20   0 22204 5036 1676 S    0  0.1   0:00.15 bash
\[...\]

顶部的摘要显示主内存 (`Mem`) 和虚拟内存 (`Swap`) 的总内存、已用内存和可用内存。还显示了缓冲区高速缓存 (`buffers`) 和页面高速缓存 (`cached`) 的大小。

在此示例中，每个进程的输出已使用 `-o` 设置排序列在 `%MEM` 上进行排序。此示例中最大的进程是 `node`，使用 2.2 GB 的主内存和近 3 GB 的虚拟内存。

主内存百分比列 (`%MEM`)、虚拟内存大小 (`VIRT`) 和驻留集大小 (`RES`) 与前面描述的 ps(1) 中的等效列具有相同的含义。有关 top(1) 内存统计信息的更多详细信息，请参阅 top(1) 手册页中的“Linux 内存类型”部分，其中解释了每个可能的内存列显示的内存类型。您还可以在使用 top(1) 时键入“`?`”来查看其内置的交互式命令摘要。

#### 7.5.9 映射

pmap(1) 命令列出进程的内存映射，显示其大小、权限和映射对象。这允许更详细地检查进程内存使用情况，并量化共享内存。

例如，在基于 Linux 的系统上：

点击这里查看代码图片

\# **pmap -x 5187**
5187:   /usr/sbin/mysqld
Address           Kbytes     RSS   Dirty Mode  Mapping
000055dadb0dd000   58284   10748       0 r-x-- mysqld
000055dade9c8000    1316    1316    1316 r---- mysqld
000055dadeb11000    3592     816     764 rw--- mysqld
000055dadee93000    1168    1080    1080 rw---   \[ anon \]
000055dae08b5000    5168    4836    4836 rw---   \[ anon \]
00007f018c000000    4704    4696    4696 rw---   \[ anon \]
00007f018c498000   60832       0       0 -----   \[ anon \]
00007f0190000000     132      24      24 rw---   \[ anon \]
\[...\]
00007f01f99da000       4       4       0 r---- ld-2.30.so
00007f01f99db000     136     136       0 r-x-- ld-2.30.so
00007f01f99fd000      32      32       0 r---- ld-2.30.so
00007f01f9a05000       4       0       0 rw-s- \[aio\] (deleted)
00007f01f9a06000       4       4       4 r---- ld-2.30.so
00007f01f9a07000       4       4       4 rw--- ld-2.30.so
00007f01f9a08000       4       4       4 rw---   \[ anon \]
00007ffd2c528000     132      52      52 rw---   \[ stack \]
00007ffd2c5b3000      12       0       0 r----   \[ anon \]
00007ffd2c5b6000       4       4       0 r-x--   \[ anon \]
ffffffffff600000       4       0       0 --x--   \[ anon \]
---------------- ------- ------- -------
total kB         1828228  450388  434200

这显示了MySQL数据库服务器的内存映射，包括虚拟内存（`Kbytes`）、主内存（`RSS`）、私有匿名内存（`Anon`）和权限（`Mode`） }）。对于许多映射，很少有内存是匿名的，并且许多映射是只读的 (`r-...`)，允许这些页面与其他进程共享。对于系统库来说尤其如此。此示例中消耗的大部分内存位于堆中，如第一波 `[ anon ]` 段（在此输出中被截断）所示。

`-x` 选项打印扩展字段。还有 `-X` 用于获取更多详细信息，而 `-XX` 用于获取内核提供的“所有内容”。仅显示这些模式的标题：

点击这里查看代码图片

\# **pmap -X $(pgrep mysqld) | head -2**
5187:   /usr/sbin/mysqld
         Address Perm   Offset Device   Inode    Size    Rss    **Pss** Referenced
Anonymous LazyFree ShmemPmdMapped Shared\_Hugetlb **Private\_Hugetlb Swap** SwapPss Locked
THPeligible ProtectionKey Mapping
\[...\]
# **pmap -XX  $(pgrep mysqld) | head -2**
5187:   /usr/sbin/mysqld
         Address Perm   Offset Device   Inode    Size KernelPageSize MMUPageSize
Rss    Pss Shared\_Clean Shared\_Dirty Private\_Clean Private\_Dirty Referenced Anonymous
LazyFree AnonHugePages ShmemPmdMapped Shared\_Hugetlb Private\_Hugetlb Swap SwapPss
Locked THPeligible ProtectionKey                 VmFlags Mapping
\[...\]

这些额外字段取决于内核版本。它们包括大页面使用、交换使用以及映射的比例集大小 (`Pss`) 的详细信息（突出显示）。 PSS 显示映射有多少私有内存，加上共享内存除以用户数。这为主内存使用提供了更实际的值。

#### 7.5.10 性能

perf(1) 是官方的 Linux 分析器，是一个具有多种功能的多功能工具。第 13 章提供了 perf(1) 的摘要。本节介绍其在内存分析中的用法。另请参阅第 6 章了解内存 PMC 的 perf(1) 分析。

##### 单衬管

以下单行代码都很有用，并且演示了内存分析的不同 perf(1) 功能。

示例页面错误（RSS 增长）以及系统范围内的堆栈跟踪，直到 Ctrl-C：

点击这里查看代码图片

perf record -e page-faults -a -g

记录 PID 1843 的所有页面错误以及堆栈跟踪，持续 60 秒：

点击这里查看代码图片

perf record -e page-faults -c 1 -p 1843 -g -- sleep 60

通过 brk(2) 记录堆增长，直到 Ctrl-C：

点击这里查看代码图片

perf record -e syscalls:sys\_enter\_brk -a -g

记录 NUMA 系统上的页面迁移：

点击这里查看代码图片

perf record -e migrate:mm\_migrate\_pages -a

计算所有 kmem 事件，每秒打印一份报告：

点击这里查看代码图片

perf stat -e 'kmem:\*' -a -I 1000

统计所有 vmscan 事件，每秒打印一份报告：

点击这里查看代码图片

perf stat -e 'vmscan:\*' -a -I 1000

计算所有内存压缩事件，每秒打印一份报告：

点击这里查看代码图片

perf stat -e 'compaction:\*' -a -I 1000

使用堆栈跟踪跟踪 kswapd 唤醒事件，直到 Ctrl-C：

点击这里查看代码图片

perf record -e vmscan:mm\_vmscan\_wakeup\_kswapd -ag

分析给定命令的内存访问：

perf mem record _command_

总结一下内存概况：

perf mem report

对于记录或采样事件的命令，请使用 `perf report` 来汇总配置文件，或使用 `perf script --header` 来打印全部内容。

请参阅第 13 章，perf，第 13.2 节，单行代码，了解更多 perf(1) 单行代码，以及第 7.5.13 节，bpftrace，它在许多相同的事件上构建可观察性程序。

##### 页面错误采样

perf(1) 可以记录页面错误的堆栈跟踪，显示触发此事件的代码路径。由于页面错误是在进程增加其驻留集大小 (RSS) 时发生的，因此分析它们可以解释为什么进程的主内存正在增长。请参见图 7.2 了解内存使用过程中页面错误的作用。

在以下示例中，通过堆栈跟踪 (`-g`) 在所有 CPU (`-a`[^9]) 上跟踪页面错误软件事件 60 秒，然后打印堆栈：

[^9]\-a 选项成为 Linux 4.11 中的默认选项。

点击这里查看代码图片

\# **perf record -e page-faults -a -g -- sleep 60**
\[ perf record: Woken up 4 times to write data \]
\[ perf record: Captured and wrote 1.164 MB perf.data (2584 samples) \]
# **perf script**
\[...\]
sleep  4910 \[001\] 813638.716924:          1 page-faults:
        ffffffff9303f31e \_\_clear\_user+0x1e (\[kernel.kallsyms\])
        ffffffff9303f37b clear\_user+0x2b (\[kernel.kallsyms\])
        ffffffff92941683 load\_elf\_binary+0xf33 (\[kernel.kallsyms\])
        ffffffff928d25cb search\_binary\_handler+0x8b (\[kernel.kallsyms\])
        ffffffff928d38ae \_\_do\_execve\_file.isra.0+0x4fe (\[kernel.kallsyms\])
        ffffffff928d3e09 \_\_x64\_sys\_execve+0x39 (\[kernel.kallsyms\])
        ffffffff926044ca do\_syscall\_64+0x5a (\[kernel.kallsyms\])
        ffffffff9320008c entry\_SYSCALL\_64\_after\_hwframe+0x44 (\[kernel.kallsyms\])
            7fb53524401b execve+0xb (/usr/lib/x86\_64-linux-gnu/libc-2.30.so)
\[...\]
mysqld  4918 \[000\] 813641.075298:          1 page-faults:
            7fc6252d7001 \[unknown\] (/usr/lib/x86\_64-linux-gnu/libc-2.30.so)
            562cacaeb282 pfs\_malloc\_array+0x42 (/usr/sbin/mysqld)
            562cacafd582 PFS\_buffer\_scalable\_container<PFS\_prepared\_stmt, 1024, 1024,
PFS\_buffer\_default\_array<PFS\_prepared\_stmt>,
PFS\_buffer\_default\_allocator<PFS\_prepared\_stmt> >::allocate+0x262 (/usr/sbin/mysqld)
            562cacafd820 create\_prepared\_stmt+0x50 (/usr/sbin/mysqld)
            562cacadbbef \[unknown\] (/usr/sbin/mysqld)
            562cab3719ff mysqld\_stmt\_prepare+0x9f (/usr/sbin/mysqld)
            562cab3479c8 dispatch\_command+0x16f8 (/usr/sbin/mysqld)
            562cab348d74 do\_command+0x1a4 (/usr/sbin/mysqld)
            562cab464fe0 \[unknown\] (/usr/sbin/mysqld)
            562cacad873a \[unknown\] (/usr/sbin/mysqld)
            7fc625ceb669 start\_thread+0xd9 (/usr/lib/x86\_64-linux-gnu/libpthread-
2.30.so)
\[...\]

这里只包含了两个堆栈。第一个来自 perf(1) 调用的虚拟 sleep(1) 命令，第二个是 MySQL 服务器。在系统范围内跟踪时，您可能会看到许多来自短期进程的堆栈，这些堆栈在退出之前会在内存中短暂增长，从而触发页面错误。您可以使用 `-p PID` 而不是 `-a` 来匹配进程。

完整输出为 222,582 行； perf 报告将代码路径总结为层次结构，但输出仍然是 7,592 行。火焰图可用于更有效地可视化整个轮廓。

##### 页面错误火焰图

图 7.12 显示了从前面的配置文件生成的页面错误火焰图。

![Images](/Systems%20Performance_%20Enterprise%20and%20the%20Cl%20-%20Brendan%20Gregg/images/07fig12.jpg)

图7.12 页面错误火焰图

图 7.12 火焰图显示 MySQL 服务器中一半以上的内存增长来自 JOIN::optimize() 代码路径（左侧大塔）。将鼠标悬停在 JOIN::optimize() 上显示它及其子调用导致了 3,226 个页面错误；对于 4 KB 页面，这相当于主内存增长约 12 MB。

用于生成此火焰图的命令（包括用于记录页面错误的 perf(1) 命令）为：

点击这里查看代码图片

\# **perf record -e page-faults -a -g -- sleep 60**
# **perf script --header > out.stacks**
$ **git clone https://github.com/brendangregg/FlameGraph; cd FlameGraph**
$ **./stackcollapse-perf.pl < ../out.stacks | ./flamegraph.pl --hash \\**
    **\--bgcolor=green --count=pages --title="Page Fault Flame Graph" > out.svg**

我将背景颜色设置为绿色，作为视觉提醒，这不是典型的 CPU 火焰图（黄色背景），而是内存火焰图（绿色背景）。

#### 7.5.11 drsnoop

drsnoop(8)[^10] 是一个 BCC 工具，用于跟踪释放内存的直接回收方法，显示受影响的进程和延迟：回收所需的时间。它可用于量化内存受限系统对应用程序性能的影响。例如：

[^10]来源：由 Wenbo Zhang 于 2019 年 2 月 10 日创建。

点击这里查看代码图片

\# **drsnoop -T**
TIME(s)       COMM           PID     LAT(ms) PAGES
0.000000000   java           11266      1.72    57
0.004007000   java           11266      3.21    57
0.011856000   java           11266      2.02    43
0.018315000   java           11266      3.09    55
0.024647000   acpid          1209       6.46    73
\[...\]

此输出显示了 Java 的一些直接回收，需要 1 到 7 毫秒。在量化应用程序影响时可以考虑这些回收率及其以毫秒为单位的持续时间 (`LAT(ms)`)。

该工具通过跟踪 vmscan mm\_vmscan\_direct\_reclaim\_begin 和 mm\_vmscan\_direct\_reclaim\_end 跟踪点来工作。这些预计是低频事件（通常突发发生），因此开销应该可以忽略不计。

drsnoop(8) 支持 `-T` 选项来包含时间戳，并支持 `-p PID` 来匹配单个进程。

#### 7.5.12 WSS

wss(8) 是我开发的一个实验工具，用于展示如何使用页表条目 (PTE)“访问”位来测量进程工作集大小 (WSS)。这是一项较长研究的一部分，旨在总结确定工作集大小的不同方法 \[Gregg 18c\]。我在这里包含了 wss(8)，因为工作集大小（频繁访问的内存量）是了解内存使用情况的重要指标，并且拥有带有警告的实验工具比没有工具好。

以下输出显示 wss(8) 测量 MySQL 数据库服务器 (mysqld) 的 WSS，并每秒打印累积的 WSS：

点击这里查看代码图片

\# **./wss.pl $(pgrep -n mysqld) 1**
Watching PID 423 page references grow, output every 1 seconds...
Est(s)     RSS(MB)    PSS(MB)    Ref(MB)
1.014       403.66     400.59      86.00
2.034       403.66     400.59      90.75
3.054       403.66     400.59      94.29
4.074       403.66     400.59      97.53
5.094       403.66     400.59     100.33
6.114       403.66     400.59     102.44
7.134       403.66     400.59     104.58
8.154       403.66     400.59     106.31
9.174       403.66     400.59     107.76
10.194      403.66     400.59     109.14

输出显示，到 5 秒时，mysqld 已触及大约 100 MB 的内存。 mysqld 的 RSS 为 400 MB。输出还包括该时间间隔的估计时间，其中包括设置和读取访问位 (`Est(s)`) 所花费的时间，以及比例设置大小 (PSS)，它考虑了与其他进程共享页面的情况。

该工具的工作原理是重置进程中每个页面的 PTE 访问位，暂停一段时间，然后检查这些位以查看哪些位已被设置。由于这是基于页面的，因此分辨率就是页面大小，通常为 4 KB。考虑它报告的数字已四舍五入为页面大小。

警告：此工具使用 /proc/PID/clear\_refs 和 /proc/PID/smaps，这可能会导致内核遍历页面结构时应用程序延迟稍高（例如 10%）。对于大型进程（> 100 GB），这种较高延迟的持续时间可能会持续一秒以上，在此期间该工具会消耗系统 CPU 时间。请记住这些开销。该工具还会重置引用的标志，这可能会使内核对要回收的页面感到困惑，特别是在交换处于活动状态时。此外，它还会激活一些以前可能未在您的环境中使用过的旧内核代码。首先在实验室环境中进行测试，以确保您了解开销。

#### 7.5.13 bpftrace

bpftrace 是一种基于 BPF 的跟踪器，它提供了一种高级编程语言，允许创建强大的单行代码和短脚本。它非常适合根据其他工具的线索进行自定义应用程序分析。 bpftrace 存储库包含用于内存分析的其他工具，包括 oomkill.bt \[Robertson 20\]。

bpftrace 在第 15 章 BPF 中进行了解释。本节展示了一些内存分析的示例。

##### 单线

以下单行代码很有用，并演示了不同的 bpftrace 功能。

按用户堆栈和进程对 libc malloc() 请求字节求和（高开销）：

点击这里查看代码图片

bpftrace -e 'uprobe:/lib/x86\_64-linux-gnu/libc.so.6:malloc {
    @\[ustack, comm\] = sum(arg0); }'

按用户堆栈对 PID 181 的 libc malloc() 请求字节求和（高开销）：

点击这里查看代码图片

bpftrace -e 'uprobe:/lib/x86\_64-linux-gnu/libc.so.6:malloc /pid == 181/ {
    @\[ustack\] = sum(arg0); }'

将 PID 181 的用户堆栈的 libc malloc() 请求字节显示为 2 的幂直方图（高开销）：

点击这里查看代码图片

bpftrace -e 'uprobe:/lib/x86\_64-linux-gnu/libc.so.6:malloc /pid == 181/ {
    @\[ustack\] = hist(arg0); }'

通过内核堆栈跟踪求和内核 kmem 缓存分配字节数：

点击这里查看代码图片

bpftrace -e 't:kmem:kmem\_cache\_alloc { @bytes\[kstack\] = sum(args->bytes\_alloc); }'

按代码路径计算进程堆扩展 (brk(2))：

点击这里查看代码图片

bpftrace -e 'tracepoint:syscalls:sys\_enter\_brk { @\[ustack, comm\] = count(); }'

按进程计数页面错误：

点击这里查看代码图片

bpftrace -e 'software:page-fault:1 { @\[comm, pid\] = count(); }'

通过用户级堆栈跟踪来计算用户页面错误：

点击这里查看代码图片

bpftrace -e 't:exceptions:page\_fault\_user { @\[ustack, comm\] = count(); }'

按跟踪点对 vmscan 操作进行计数：

点击这里查看代码图片

bpftrace -e 'tracepoint:vmscan:\* { @\[probe\] = count(); }'

按进程计数交换：

点击这里查看代码图片

bpftrace -e 'kprobe:swap\_readpage { @\[comm, pid\] = count(); }'

计算页面迁移：

点击这里查看代码图片

bpftrace -e 'tracepoint:migrate:mm\_migrate\_pages { @ = count(); }'

跟踪压缩事件：

点击这里查看代码图片

bpftrace -e 't:compaction:mm\_compaction\_begin { time(); }'

列出 libc 中的 USDT 探针：

点击这里查看代码图片

bpftrace -l 'usdt:/lib/x86\_64-linux-gnu/libc.so.6:\*'

列出内核 kmem 跟踪点：

bpftrace -l 't:kmem:\*'

列出所有内存子系统 (mm) 跟踪点：

bpftrace -l 't:\*:mm\_\*'

##### 用户分配堆栈

用户级分配可以从使用的分配函数中追踪。对于此示例，跟踪 libc 中的 malloc(3) 函数以查找 PID 4840（MySQL 数据库服务器）。分配请求的大小被记录为由用户级堆栈跟踪键入的直方图：

点击这里查看代码图片

\# **bpftrace -e 'uprobe:/lib/x86\_64-linux-gnu/libc.so.6:malloc /pid == 4840/ {**
    **@\[ustack\] = hist(arg0); }'**
Attaching 1 probe...
^C
\[...\]

    \_\_libc\_malloc+0
    Filesort\_buffer::allocate\_sized\_block(unsigned long)+52
    0x562cab572344
    filesort(THD\*, Filesort\*, RowIterator\*, Filesort\_info\*, Sort\_result\*, unsigned
long long\*)+4017
    SortingIterator::DoSort(QEP\_TAB\*)+184
    SortingIterator::Init()+42
    SELECT\_LEX\_UNIT::ExecuteIteratorQuery(THD\*)+489
    SELECT\_LEX\_UNIT::execute(THD\*)+266
    Sql\_cmd\_dml::execute\_inner(THD\*)+563
    Sql\_cmd\_dml::execute(THD\*)+1062
    mysql\_execute\_command(THD\*, bool)+2380
    Prepared\_statement::execute(String\*, bool)+2345
    Prepared\_statement::execute\_loop(String\*, bool)+172
    mysqld\_stmt\_execute(THD\*, Prepared\_statement\*, bool, unsigned long, PS\_PARAM\*)
+385
    dispatch\_command(THD\*, COM\_DATA const\*, enum\_server\_command)+5793
    do\_command(THD\*)+420
    0x562cab464fe0
    0x562cacad873a
    start\_thread+217
\]:
\[32K, 64K)           676 |@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@|
\[64K, 128K)          338 |@@@@@@@@@@@@@@@@@@@@@@@@@@                          |

输出显示，在跟踪时，此代码路径有 676 个 malloc() 请求，大小在 32 到 64 KB 之间，以及 338 个大小在 64 KB 到 128 KB 之间。

##### malloc() 字节火焰图

前一行的输出有很多页长，因此更容易理解为火焰图。可以通过以下步骤生成：

点击这里查看代码图片

\# **bpftrace -e 'u:/lib/x86\_64-linux-gnu/libc.so.6:malloc /pid == 4840/ {**
    **@\[ustack\] = hist(arg0); }' > out.stacks**
$ **git clone https://github.com/brendangregg/FlameGraph; cd FlameGraph**
$ **./stackcollapse-bpftrace.pl < ../out.stacks | ./flamegraph.pl --hash \\**
    **\--bgcolor=green --count=bytes --title="malloc() Bytes Flame Graph" > out.svg**

警告：用户级分配请求可能是一项频繁的活动，每秒发生数百万次。虽然检测成本很小，但当乘以高速率时，可能会在跟踪时导致显着的 CPU 开销，从而使目标速度减慢两倍或更多——请谨慎使用。因为它成本较低，所以我首先使用堆栈跟踪的 CPU 分析来获取分配路径的句柄，或者下一节中显示的页面错误跟踪。

##### 页面错误火焰图

跟踪页面错误会显示进程内存大小何时增长。前面的 malloc() 一行跟踪了分配路径。前面在第 7.5.10 节 perf 中执行了页面错误跟踪，并从中生成了火焰图。使用 bpftrace 的优点是可以在内核空间中聚合堆栈跟踪以提高效率，并且仅将唯一的堆栈和计数写入用户空间。

以下命令使用 bpftrace 收集页面错误堆栈跟踪，然后从中生成火焰图：

点击这里查看代码图片

\# **bpftrace -e 't:exceptions:page\_fault\_user { @\[ustack, comm\] = count(); }**
    **' > out.stacks**
$ **git clone https://github.com/brendangregg/FlameGraph; cd FlameGraph**
$ **./stackcollapse-bpftrace.pl < ../out.stacks | ./flamegraph.pl --hash \\**
    **\--bgcolor=green --count=pages --title="Page Fault Flame Graph" > out.svg**

有关页面错误堆栈跟踪和火焰图的示例，请参阅第 7.5.10 节 perf。

##### 内存内部结构

如果需要，您可以开发自定义工具来更深入地探索内存分配和内部结构。首先尝试跟踪内核内存事件，并尝试使用 USDT 探测库分配器（例如 libc）。列出跟踪点：

点击这里查看代码图片

\# **bpftrace -l 'tracepoint:kmem:\*'**
tracepoint:kmem:kmalloc
tracepoint:kmem:kmem\_cache\_alloc
tracepoint:kmem:kmalloc\_node
tracepoint:kmem:kmem\_cache\_alloc\_node
tracepoint:kmem:kfree
tracepoint:kmem:kmem\_cache\_free
\[...\]
# **bpftrace -l 't:\*:mm\_\*'**
tracepoint:huge\_memory:mm\_khugepaged\_scan\_pmd
tracepoint:huge\_memory:mm\_collapse\_huge\_page
tracepoint:huge\_memory:mm\_collapse\_huge\_page\_isolate
tracepoint:huge\_memory:mm\_collapse\_huge\_page\_swapin
tracepoint:migrate:mm\_migrate\_pages
tracepoint:compaction:mm\_compaction\_isolate\_migratepages
tracepoint:compaction:mm\_compaction\_isolate\_freepages
\[...\]

每个跟踪点都有可以使用 `-lv` 列出的参数。在此内核 (5.3) 上有 12 个 kmem 跟踪点，以及 47 个以“mm\_”开头的跟踪点。

列出 Ubuntu 上 libc 的 USDT 探针：

点击这里查看代码图片

\# **bpftrace -l 'usdt:/lib/x86\_64-linux-gnu/libc.so.6'**
usdt:/lib/x86\_64-linux-gnu/libc.so.6:libc:setjmp
usdt:/lib/x86\_64-linux-gnu/libc.so.6:libc:longjmp
usdt:/lib/x86\_64-linux-gnu/libc.so.6:libc:longjmp\_target
usdt:/lib/x86\_64-linux-gnu/libc.so.6:libc:lll\_lock\_wait\_private
usdt:/lib/x86\_64-linux-gnu/libc.so.6:libc:memory\_mallopt\_arena\_max
usdt:/lib/x86\_64-linux-gnu/libc.so.6:libc:memory\_mallopt\_arena\_test
usdt:/lib/x86\_64-linux-gnu/libc.so.6:libc:memory\_tunable\_tcache\_max\_bytes
\[...\]

对于此 libc 版本 (6)，有 33 个 USDT 探针。

如果跟踪点和 USDT 探针不足，请考虑使用带有 kprobes 和 uprobes 的动态检测。

还有内存观察点的观察点探针类型：读取、写入或执行指定内存地址时发生的事件。

由于内存事件可能非常频繁，因此对它们进行检测可能会消耗大量开销。来自用户空间的 malloc(3) 函数每秒可以被调用数百万次，并且由于当前的 uprobes 开销（请参阅第 4 章，可观测性工具，第 4.3.7 节，uprobes），跟踪它们可能会使目标速度减慢两倍或更多。请谨慎使用并找到减少这种开销的方法，例如使用地图来汇总统计信息而不是打印每个事件的详细信息，以及跟踪尽可能少的事件。

#### 7.5.14 其他工具

表 7.6 列出了本书其他章节以及 BPF 性能工具 \[Gregg 19\] 中包含的内存可观察性工具。

表 7.6 其他内存可观察性工具

部分

工具

描述

[[ch06_split_001.xhtml#ch06lev6sec11|6.6.11]]

普卡奇

CPU 周期使用情况，包括 LLC 未命中

[[ch06_split_001.xhtml#ch06lev6sec12|6.6.12]]

tlbstat

总结 TLB 周期

[[ch08.xhtml#ch08lev6sec2|8.6.2]]

自由的

缓存容量统计

[[ch08.xhtml#ch08lev6sec12|8.6.12]]

缓存状态

页面缓存统计

\[格雷格 19\]

杀戮

显示有关 OOM 终止事件的额外信息

\[格雷格 19\]

内存泄漏

显示可能的内存泄漏代码路径

\[格雷格 19\]

映射监听

跟踪系统范围内的 mmap(2) 调用

\[格雷格 19\]

布克堆栈

显示带有用户堆栈跟踪的 brk() 调用

\[格雷格 19\]

什姆斯努普

跟踪共享内存调用的详细信息

\[格雷格 19\]

故障

按用户堆栈跟踪显示页面错误

\[格雷格 19\]

过失

按文件名显示页面错误

\[格雷格 19\]

虚拟机扫描

测量虚拟机扫描仪收缩和回收时间

\[格雷格 19\]

交换

按进程显示交换

\[格雷格 19\]

故障

按进程显示巨大的页面错误

其他 Linux 内存可观察性工具和来源包括：

*   `dmesg`：检查来自 OOM Killer 的“内存不足”消息。
    
*   `dmidecode`：显示内存条的 BIOS 信息。
    
*   `tiptop`：top(1) 的一个版本，按进程显示 PMC 统计信息。
    
*   `valgrind`：性能分析套件，包括 memcheck，它是用户级分配器的包装器，用于内存使用分析（包括泄漏检测）。这会花费大量的开销；手册建议它可能导致目标运行速度慢 20 到 30 倍 \[Valgrind 20\]。
    
*   `iostat`：如果交换设备是物理磁盘或分片，则可以使用 iostat(1) 观察设备 I/O，这表明系统正在分页。
    
*   `/proc/zoneinfo`：内存区域（DMA等）的统计信息。
    
*   `/proc/buddyinfo`：页面的内核伙伴分配器的统计信息。
    
*   `/proc/pagetypeinfo`：内核空闲内存页统计信息；可用于帮助调试内核内存碎片问题。
    
*   `/sys/devices/system/node/node*/numastat`：NUMA 节点的统计信息。
    
*   SysRq `m`：Magic SysRq 有一个“m”键，可以将内存信息转储到控制台。
    

以下是 dmidecode(8) 的输出示例，显示了一组内存：

点击这里查看代码图片

\# **dmidecode**
\[...\]
Memory Device
        Array Handle: 0x0003
        Error Information Handle: Not Provided
        Total Width: 64 bits
        Data Width: 64 bits
        Size: 8192 MB
        Form Factor: SODIMM
        Set: None
        Locator: ChannelA-DIMM0
        Bank Locator: BANK 0
        Type: DDR4
        Type Detail: Synchronous Unbuffered (Unregistered)
        Speed: 2400 MT/s
        Manufacturer: Micron
        Serial Number: 00000000
        Asset Tag: None
        Part Number: 4ATS1G64HZ-2G3A1
        Rank: 1
        Configured Clock Speed: 2400 MT/s
        Minimum Voltage: Unknown
        Maximum Voltage: Unknown
        Configured Voltage: 1.2 V
\[...\]

此输出对于静态性能调整来说是有用的信息（例如，它显示类型是 DDR4 而不是 DDR5）。不幸的是，云访客通常无法获得此信息。

以下是 SysRq“m”触发器的一些示例输出：

点击这里查看代码图片

\# **echo m > /proc/sysrq-trigger**
# **dmesg**
\[...\]
\[334849.389256\] sysrq: Show Memory
\[334849.391021\] Mem-Info:
\[334849.391025\] active\_anon:110405 inactive\_anon:24 isolated\_anon:0
                 active\_file:152629 inactive\_file:137395 isolated\_file:0
                 unevictable:4572 dirty:311 writeback:0 unstable:0
                 slab\_reclaimable:31943 slab\_unreclaimable:14385
                 mapped:37490 shmem:186 pagetables:958 bounce:0
                 free:37403 free\_pcp:478 free\_cma:2289
\[334849.391028\] Node 0 active\_anon:441620kB inactive\_anon:96kB active\_file:610516kB
inactive\_file:549580kB unevictable:18288kB isolated(anon):0kB isolated(file):0kB
mapped:149960kB dirty:1244kB writeback:0kB shmem:744kB shmem\_thp: 0kB
shmem\_pmdmapped: 0kB anon\_thp: 2048kB writeback\_tmp:0kB unstable:0kB
all\_unreclaimable? no
\[334849.391029\] Node 0 DMA free:12192kB min:360kB low:448kB high:536kB ...
\[...\]

如果系统已锁定，这可能很有用，因为仍然可以使用控制台键盘上的 SysRq 键序列请求此信息（如果可用）\[Linux 20g\]。

应用程序和虚拟机（例如，Java VM）也可以提供它们自己的内存分析工具。请参阅第 5 章“应用”。

### 7.6 调优

您可以做的最重要的内存调整是确保应用程序保留在主内存中，并且分页和交换不会频繁发生。第 7.4 节“方法论”和第 7.5 节“可观测性工具”介绍了如何识别此问题。本节讨论其他内存调整：内核可调参数、配置大页面、分配器和资源控制。

调整的细节（可用选项以及将其设置为什么）取决于操作系统版本和预期工作负载。以下部分按调整类型组织，提供了可用的可调参数以及为什么需要调整它们的示例。

#### 7.6.1 可调参数

本节介绍最新 Linux 内核的可调参数示例。

Documentation/sysctl/vm.txt 中的内核源文档描述了各种内存可调参数，并且可以使用 sysctl(8) 进行设置。表 7.7 中的示例来自 5.3 内核，默认值来自 Ubuntu 19.10（本书第一版中列出的内容从那时起就没有改变）。

表 7.7 Linux 内存可调参数示例

选项

默认

描述

vm.dirty\_background\_bytes

0

触发 pdflush 后台写回的脏内存量

vm.dirty\_background\_ratio

10

触发 pdflush 后台写回的脏系统内存百分比

vm.dirty\_bytes

0

导致写入进程开始回写的脏内存量

vm.dirty\_ratio

20

导致写入进程开始回写的脏系统内存比率

vm.dirty\_expire\_centisecs

3,000

脏内存符合 pdflush 条件的最短时间（促进写入取消）

vm.dirty\_writeback\_centisecs

500

pdflush 唤醒间隔（0 表示禁用）

vm.min\_free\_kbytes

动态的

设置所需的可用内存量（某些内核原子分配可能会消耗它）

vm.watermark\_scale\_factor

10

控制唤醒和睡眠的 kswapd 水印之间的距离（最小、低、高）（单位是 10000 的分数，例如 10 表示系统内存的 0.1%）

vm.watermark\_boost\_factor

5000

当内存出现碎片时（最近发生碎片事件），kswapd 会扫描超过高水位线的距离；单位是 10000 的分数，所以 5000 意味着 kswapd 可以提升至高水位线的 150%； 0 禁用

vm.percpu\_pagelist\_fraction

0

这可以覆盖可分配给每个 cpu 页面列表的页面的默认最大分数（值 10 限制为页面的 1/10）

虚拟机内存过量使用

0

0 = 使用启发式方法允许合理的过度使用； 1 = 总是过量使用； 2 = 不要过度投入

虚拟机交换性

60

有利于通过交换（分页）释放内存而不是从页面缓存中回收内存的程度

vm.vfs\_cache\_压

100

回收缓存目录和inode对象的程度；较低的值更能保留他们； 0 表示从不回收——很容易导致内存不足的情况

内核.numa\_balancing

1

启用自动 NUMA 页面平衡

kernel.numa\_balancing\_scan\_size\_mb

256

每次 NUMA 平衡扫描扫描多少 MB 的页面

可调参数使用包含单位的一致命名方案。请注意，dirty\_background\_bytes 和 dirty\_background\_ratio 是互斥的，dirty\_bytes 和 dirty\_ratio 也是互斥的（当设置其中一个时，它将覆盖另一个）。

vm.min\_free\_kbytes 的大小动态设置为主内存的一部分。选择此算法的算法不是线性的，因为对可用内存的需求并不与主内存大小线性缩放。 （作为参考，Linux 源代码中的 mm/page\_alloc.c 对此进行了记录。）可以减少 vm.min\_free\_kbytes 来为应用程序释放一些内存，但这也可能导致内核在内存压力期间不堪重负，并诉诸于使用早点OOM。增加它可以帮助避免 OOM 杀死。

避免 OOM 的另一个参数是 vm.overcommit\_memory，可以将其设置为 2 以禁用过度使用并避免导致 OOM 的情况。如果您想在每个进程的基础上控制 OOM Killer，请检查您的内核版本中的 /proc 可调参数，例如 oom\_adj 或 oom\_score\_adj。这些在 Documentation/filesystems/proc.txt 中进行了描述。

如果 vm.swappiness 可调参数比预期更早地开始交换应用程序内存，则会显着影响性能。该可调参数的值可以在 0 到 100 之间，较高的值有利于交换应用程序，从而保留页面缓存。可能需要将其设置为零，以便以牺牲页面缓存为代价尽可能长时间地保留应用程序内存。当内存仍然不足时，内核仍然可以使用交换。

在 Netflix，早期内核（大约 Linux 3.13）的 kernel.numa\_balancing 设置为零，因为过度激进的 NUMA 扫描消耗了太多 CPU \[Gregg 17d\]。这个问题在后来的内核中得到了修复，并且还有其他可调参数，包括 kernel.numa\_balancing\_scan\_size\_mb 用于调整 NUMA 扫描的激进程度。

#### 7.6.2 多种页面大小

大页面大小可以通过提高 TLB 缓存的命中率（增加其范围）来提高内存 I/O 性能。大多数现代处理器支持多种页面大小，例如 4 KB 默认页面和 2 MB 大页面。

在 Linux 上，可以通过多种方式配置大页面（称为巨页）。有关参考，请参阅 Documentation/vm/hugetlbpage.txt。

这些通常从创建大页面开始：

点击这里查看代码图片

\# **echo 50 > /proc/sys/vm/nr\_hugepages**
# **grep Huge /proc/meminfo**
AnonHugePages:         0 kB
HugePages\_Total:      50
HugePages\_Free:       50
HugePages\_Rsvd:        0
HugePages\_Surp:        0
Hugepagesize:       2048 kB

应用程序消耗大页面的一种方法是通过共享内存段以及 shmget(2) 的 SHM\_HUGETLBS 标志。另一种方法涉及创建一个巨大的基于页面的文件系统，供应用程序映射内存：

点击这里查看代码图片

\# **mkdir /mnt/hugetlbfs**
# **mount -t hugetlbfs none /mnt/hugetlbfs -o pagesize=2048K**

其他方法包括 mmap(2) 的 MAP\_ANONYMOUS|MAP\_HUGETLB 标志以及使用 libhugetlbfs API \[Gorman 10\]。

最后，透明大页面 (THP) 是另一种使用大页面的机制，通过自动将普通页面提升和降级为大页面，而不需要应用程序指定大页面 \[Corbet 11\]。在 Linux 源中，请参阅 Documentation/vm/transhuge.txt 和 admin-guide/mm/transhuge.rst。[^11]

[^11]请注意，历史上透明大页面存在性能问题，阻碍了其使用。这些问题有望得到解决。

#### 7.6.3 分配器

可以使用不同的用户级分配器，从而为多线程应用程序提供改进的性能。这些可以在编译时选择，或者在执行时通过设置 LD\_PRELOAD 环境变量来选择。

例如，可以使用以下方式选择 libtcmalloc 分配器：

点击这里查看代码图片

export LD\_PRELOAD=/usr/lib/x86\_64-linux-gnu/libtcmalloc\_minimal.so.4

这可以放置在其启动脚本中。

#### 7.6.4 NUMA 绑定

在 NUMA 系统上，numactl(8) 命令可用于将进程绑定到 NUMA 节点。这可以提高不需要多个主内存 NUMA 节点的应用程序的性能。用法示例：

\# **numactl --membind=0 3161**

这会将 PID 3161 绑定到 NUMA 节点 0。如果此节点无法满足这些要求，则此进程的未来内存分配将失败。使用此选项时，您还应该研究使用 `--physcpubind` 选项来限制连接到该 NUMA 节点的 CPU 的 CPU 使用率。我通常使用 NUMA 和 CPU 绑定来将进程限制到单个套接字，以避免 CPU 互连访问的性能损失。

使用 numastat(8)（第 7.5.6 节）列出可用的 NUMA 节点。

#### 7.6.5 资源控制

使用 ulimit(1) 可以实现基本资源控制，包括设置主内存限制和虚拟内存限制。

对于 Linux，容器组 (cgroups) 内存子系统提供各种附加控制。这些包括：

*   `memory.limit_in_bytes`：允许的最大用户内存，包括文件缓存使用量，以字节为单位
    
*   `memory.memsw.limit_in_bytes`：允许的最大内存和交换空间，以字节为单位（当使用交换时）
    
*   `memory.kmem.limit_in_bytes`：允许的最大内核内存，以字节为单位
    
*   `memory.tcp.limit_in_bytes`：最大 tcp 缓冲区内存，以字节为单位。
    
*   `memory.swappiness`：与前面描述的 vm.swappiness 类似，但可以为 cgroup 设置
    
*   `memory.oom_control`：可以设置为 0，以允许此 cgroup 的 OOM Killer，或设置为 1，以禁用它
    

Linux 还允许在 /etc/security/limits.conf 中进行系统范围的配置。

有关资源控制的更多信息，请参阅第 11 章，云计算。

### 7.7 练习

1.  回答以下有关内存术语的问题：
    
    *   什么是内存页？
        
    *   什么是常驻记忆？
        
    *   什么是虚拟内存？
        
    *   使用 Linux 术语，分页和交换有什么区别？
        
2.  回答以下概念性问题：
    
    *   需求调页的目的是什么？
        
    *   描述内存利用率和饱和度。
        
    *   MMU 和 TLB 的目的是什么？
        
    *   页面调出守护进程的作用是什么？
        
    *   OOM杀手的作用是什么？
        
3.  回答以下更深层次的问题：
    
    *   什么是匿名分页，为什么分析它比文件系统分页更重要？
        
    *   描述当基于 Linux 的系统上的可用内存耗尽时，内核为释放更多内存而采取的步骤。
        
    *   描述基于slab的分配的性能优势。
        
4.  为您的操作系统开发以下过程：
    
    *   内存资源的使用方法清单。包括如何获取每个指标（例如，要执行哪个命令）以及如何解释结果。在安装或使用其他软件产品之前，尝试使用现有的操作系统可观察性工具。
        
    *   创建内存资源的工作负载特征检查表。包括如何获取每个指标，并首先尝试使用现有的操作系统可观察性工具。
        
5.  执行这些任务：
    
    *   选择一个应用程序，并总结导致内存分配 (malloc(3)) 的代码路径。
        
    *   选择具有一定程度内存增长的应用程序（调用 brk(2) 或 sbrk(2)），并总结导致这种增长的代码路径。
        
    *   仅描述以下 Linux 输出中可见的内存活动：
        
        点击这里查看代码图片
        
        \# **vmstat 1**
        procs -----------memory-------- ---swap-- -----io---- --system-- -----cpu-----
         r  b   swpd   free  buff cache   si   so    bi    bo   in   cs us sy id wa st
         2  0 413344  62284    72  6972    0    0    17    12    1    1  0  0 100  0  0
         2  0 418036  68172    68  3808    0 4692  4520  4692 1060 1939 61 38  0  1  0
         2  0 418232  71272    68  1696    0  196 23924   196 1288 2464 51 38  0 11  0
         2  0 418308  68792    76  2456    0   76  3408    96 1028 1873 58 39  0  3  0
         1  0 418308  67296    76  3936    0    0  1060     0 1020 1843 53 47  0  0  0
         1  0 418308  64948    76  3936    0    0     0     0 1005 1808 36 64  0  0  0
         1  0 418308  62724    76  6120    0    0  2208     0 1030 1870 62 38  0  0  0
         1  0 422320  62772    76  6112    0 4012     0  4016 1052 1900 49 51  0  0  0
         1  0 422320  62772    76  6144    0    0     0     0 1007 1826 62 38  0  0  0
         1  0 422320  60796    76  6144    0    0     0     0 1008 1817 53 47  0  0  0
         1  0 422320  60788    76  6144    0    0     0     0 1006 1812 49 51  0  0  0
         3  0 430792  65584    64  5216    0 8472  4912  8472 1030 1846 54 40  0  6  0
         1  0 430792  64220    72  6496    0    0  1124    16 1024 1857 62 38  0  0  0
         2  0 434252  68188    64  3704    0 3460  5112  3460 1070 1964 60 40  0  0  0
         2  0 434252  71540    64  1436    0    0 21856     0 1300 2478 55 41  0  4  0
         1  0 434252  66072    64  3912    0    0  2020     0 1022 1817 60 40  0  0  0
        \[...\]
        
6.  （可选，高级）查找或开发指标来显示内核 NUMA 内存局部性策略在实践中的效果如何。开发具有良好或较差内存局部性的“已知”工作负载来测试指标。
    

### 7.8 参考文献

\[Corbató 68\] Corbató, F. J.，Multics 系统的寻呼实验，麻省理工学院 MAC 项目报告 MAC-M-384，1968。

\[Denning 70\] Denning, P.，“虚拟内存”，ACM 计算调查 (CSUR) 2，第 1 期。 1970 年 3 月。

\[Peterson 77\] Peterson, J. 和 Norman, T.，“Buddy Systems”，ACM 通信，1977 年。

\[Thompson 78\] Thompson, K.，UNIX 实现，贝尔实验室，1978 年。

\[Babaoglu 79\] Babaoglu, O.、Joy, W. 和 Porcar, J.，UNIX 操作系统伯克利虚拟内存扩展的设计和实现，加州大学电气工程和计算机科学系计算机科学分部，伯克利，1979。

\[Bach 86\] Bach, M. J.，UNIX 操作系统的设计，Prentice Hall，1986。

\[Bonwick 94\] Bonwick, J.，“Slab 分配器：对象缓存内核内存分配器”，USENIX，1994。

\[Bonwick 01\] Bonwick, J. 和 Adams, J.，“杂志和 Vmem：将 Slab 分配器扩展到许多 CPU 和任意资源”，USENIX，2001 年。

\[Corbet 04\] Corbet, J.，“2.6 交换行为”，LWN.net，http://lwn.net/Articles/83588，2004

\[Gorman 04\] Gorman, M.，了解 Linux 虚拟内存管理器，Prentice Hall，2004 年。

\[McDougall 06a\] McDougall, R.、Mauro, J. 和 Gregg, B.，Solaris 性能和工具：Solaris 10 和 OpenSolaris 的 DTrace 和 MDB 技术，Prentice Hall，2006 年。

\[Ghemawat 07\] Ghemawat, S.，“TCMalloc：线程缓存 Malloc”，https://gperftools.github.io/gperftools/tcmalloc.html，2007 年。

\[Lameter 07\] Lameter, C.，“SLUB：未排队的平板分配器 V6”，Linux 内核邮件列表，http://lwn.net/Articles/229096，2007 年。

\[Hall 09\] Hall, A.，“感谢 Linux 的记忆”，Andrew Hall，https://www.ibm.com/developerworks/library/j-nativememory-linux，2009 年。

\[Gorman 10\] Gorman, M.，“大页面第 2 部分：接口”，LWN.net，http://lwn.net/Articles/375096，2010。

\[Corbet 11\] Corbet, J.，“2.6.38 中的透明大页”，LWN.net，http://lwn.net/Articles/423584，2011。

\[Facebook 11\]“使用 jemalloc 进行可扩展内存分配”，Facebook 工程，https://www.facebook.com/notes/facebook-engineering/scalable-memory-allocation-using-jemalloc/480222803919，2011。

\[Evans 17\] Evans, J.，“交换、内存限制和 cgroup”，https://jvns.ca/blog/2017/02/17/mystery-swap，2017 年。

\[Gregg 17d\] Gregg, B.，“AWS re:Invent 2017：Netflix 如何调整 EC2”，http://www.brendangregg.com/blog/2017-12-31/reinvent-netflix-ec2-tuning.html， 2017年。

\[Corbet 18a\] Corbet, J.，“是时候删除 ZONE\_DMA 了吗？” LWN.net，https://lwn.net/Articles/753273，2018。

\[Crucial 18\]“RAM 速度和 CAS 延迟之间的差异”，https://www.crucial.com/articles/about-memory/difference- Between-speed-and-latency，2018 年。

\[Gregg 18c\] Gregg, B.，“工作集大小估计”，http://www.brendangregg.com/wss.html，2018 年。

\[Facebook 19\]“PSI 入门”，Facebook 工程，https://facebookmicrosites.github.io/psi/docs/overview，2019。

\[Gregg 19\] Gregg, B.，BPF 性能工具：Linux 系统和应用程序可观察性，Addison-Wesley，2019 年。

\[英特尔 19a\] 英特尔 64 和 IA-32 架构软件开发人员手册，合并卷：1、2A、2B、2C、3A、3B 和 3C，英特尔，2019 年。

\[Amazon 20\]“Amazon EC2 高内存实例”，https://aws.amazon.com/ec2/instance-types/high-memory，2020 年访问。

\[Linux 20g\]“Linux Magic System Request Key Hacks”，Linux 文档，https://www.kernel.org/doc/html/latest/admin-guide/sysrq.html，2020 年访问。

\[Robertson 20\] Robertson, A.，“bpftrace”，https://github.com/iovisor/bpftrace，最后更新于 2020 年。

\[Valgrind 20\]“Valgrind 文档”，http://valgrind.org/docs/manual，2020 年 5 月。